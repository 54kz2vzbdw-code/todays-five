# Claims inventory — the Apple side, `0e03143..7341981`

- Worktree: `/Users/pricebrannen/Today's Five/todays-five-review-claims-apple`, detached at `[REDACTED]` (`git rev-parse HEAD`), read-only; nothing under it was edited.
- Written 2026-09-10 (this session started at 14:36 with the 1-minute load at 37 and the 5-minute at 116). **No xcodebuild, no `swift test`, no simulator, no build of any kind was run.** Every "measured now" cell below is git, `python3` (+ fontTools 4.60.2), the off-PATH `node` v24, or `PlistBuddy`/`otool`/`strings`/`codesign` against the baseline's own products (`/tmp/tf-review-baseline`, `/tmp/tf-review/7341981.xcarchive`) — or a reuse of the baseline's and the Watch track's tallies, cited by path.
- Evidence: `E/` = `review/raw/claims-apple/` (the two predecessors' 37 files, read in full, plus this session's `z-*.txt`: `z-tf-debug-gates`, `z-pbxproj-ids`, `z-woff2-family-names`, `z-contrast-arithmetic`, `z-csstext-diff`, `z-code-comment-figures`, `z-last-checks`, `z-commit-counts`, `z-misc-greps`); `B/` = `review/raw/baseline/` (+ `baseline.md`); `W/` = `review/raw/watch/` (+ `watch.md`). Instruments the predecessors left are in `E/tools/` (`kit-tokens.mjs`, `print-audit.py`); mine are in the scratchpad (`claims-apple2/{tfgates.py,contrast.mjs,cssdiff2.mjs}`) and their exact output is in the `z-` files.
- Columns: **claimed** · **where** (file:line at 7341981, or commit) · **how measured then** (what the write-up says its instrument was) · **how measured now** (instrument, command, evidence path) · **verdict**. Verdicts start with one of three words: **holds**, **doesn't hold**, **can't**. A claim that was true when written and has since been superseded by the round's own later work is "holds then; superseded" — counted under *holds* — unless the stale sentence still stands somewhere a reader will take as current, in which case it is *doesn't hold* and says so.
- "Quoting the write-up back is not a measurement": where the only instrument was the write-up's own printout and nothing here can re-run it, the verdict is *can't*, with what would settle it.

---

## A. `apple/PLAN-apple-phase3.md`

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| A1 | Xcode 26.6 (17F113), Swift 6.3.3 | :24 | `xcodebuild -version` | `E/environment-and-archives.txt`: Xcode 26.6 / 17F113; swift 6.3.3 | **holds** |
| A2 | runtimes iOS 18.1 / 26.5, watchOS 11.1 / 26.5 | :25 | `simctl list runtimes` | same file, runtimes line | **holds** |
| A3 | three pre-existing 18.1+11.1 pairs; a new 26.5 pair (S11 46mm + iPhone 17) `active, connected` | :26 | `simctl list pairs` | `E/misc-checks.txt`: three 18.1/11.1 pairs (Shutdown) + `4B6A97F5` S11 46mm + iPhone 17 `(active, connected)` | **holds** |
| A4 | git clean on `main`, web at 1.12 build 158, **160 commits** | :27 | git | `git rev-list --count e215e65` = 160 (the last main commit before 7d9515f "Phase 3 begins" = 161); archive `TodaysFive 1.12 (158) Watch.xcarchive` with 1.12 (158) in all three plists — `E/z-commit-counts.txt`, `E/environment-and-archives.txt` | **holds** |
| A5 | baseline `swift test` 94 tests in 7 suites | :30 | `swift test` | `E/test-counts-corrected.txt`: e215e65 `@Test=94 @Suite=7` | **holds** |
| A6 | WatchConnectivity works between paired simulators (`activation=2 paired=true installed=true`) | :34-38 | throwaway probe | not re-run; the Watch track's hand-off on the review pair delivered a stamped payload both ways (`W/handoff-phone.log`: `sent v=1 links=0 …`; `W/handoff-watch.log`: `heard v=1 links=0 applied=true`) | **holds** (the channel; the probe's own printout not reproduced) |
| A7 | `simctl install` of the phone app leaves `isWatchAppInstalled == false`; every `[REDACTED]` throws 7006 | :41-44 | probe | not re-provoked; the baseline installed the watch app explicitly (`B/sim-summary.txt` `install watch app: (rc=0)`) and the hand-off then worked; `E/sdk-on-disk.txt`: `[REDACTED] = 7006` | **can't** (the failure not re-provoked; consistent) |
| A8 | `[REDACTED]` empty at activation in one run, populated in another; two callbacks vs eleven-for-eleven | :45-56 | two probes | not re-measured; the receiver reads both doors; `WatchLinkReceiver.swift:218-219` still says "Measured empty in every run" — `watch.md` F8 | **can't** (and the code comment contradicts the plan's own softening) |
| A9 | `[REDACTED] = 262144`; 65,536 for `sendMessage` / `transferUserInfo`; identical in the iOS and watchOS runtimes | :57-58 | "on disk" | Watch track: `nm`/`otool -l`/`xxd` on the watchOS 26.5 runtime's `WatchConnectivity` binary → 262144 / 65536 / 65536 (`W/wc-payload-size-limits.txt`); not a public header constant (`E/sdk-on-disk-2.txt`); the iOS runtime not re-read | **holds** (watchOS half; iOS half not re-read) |
| A10 | `isReachable` asymmetric | :60-63 | probe | nothing in shipped code reads it (`watch.md`: grep → nothing) | **can't** (not needed) |
| A11 | ConfigGen "Multiple commands produce …" once two targets link the package; work dir not keyed by platform; prebuild command; `tfconfiggen` gone | :65-83 | a failing build | `Package.swift` two `.buildTool()` plugins, `ConfigGen.swift:6` "prebuild command over /bin/sh"; `E/files-and-shots.txt`: `D apple/TodaysFiveCore/Sources/tfconfiggen/main.swift`; baseline 5a–5e built with both plugins on one target, 0 errors (`baseline.md` §5) | **holds** (the collision itself not re-provoked) |
| A12 | App Manager key cannot add App Groups; the Apple ID did; group now in the watch app's signed entitlements | :85-99 | xcodebuild errors | `E/entitlements.txt` (`codesign -d --entitlements`): `group.com.pricebrannen.todaysfive` in the archive's Watch app and appex; the key wall not re-provoked | **holds** (entitlement present); the wall **can't** |
| A13 | neither `Speech.framework` nor `CoreHaptics.framework` is in the watchOS SDK | :101-105, :341 | on disk | `E/sdk-on-disk.txt`: Speech watchOS NO / watchsim NO / iOS yes; CoreHaptics NO / NO / yes; `E/built-products.txt`: archive Watch binary links no CoreHaptics | **holds** |
| A14 | deployment watchOS 11.0; `.handGestureShortcut` is `@available(watchOS 11.0, *)`; package floor watchOS 10 | :119 | pbxproj / SDK | `E/pbxproj-and-plists.txt` `WATCHOS_DEPLOYMENT_TARGET = 11.0` ×4; `E/sdk-on-disk.txt` `@available(… watchOS 11.0, *)` on `handGestureShortcut`; `Package.swift` `.watchOS(.v10)` | **holds** |
| A15 | complications appex embedded in the **watch** app | :120 | archive | `baseline.md`: `…/TodaysFive.app/Watch/TodaysFiveWatch.app/PlugIns/[REDACTED].appex` | **holds** |
| A16 | `MARKETING_VERSION 1.12`, `CURRENT_PROJECT_VERSION 158` on all three bundles | :121, :435 | pbxproj | true at 4962f97 (the 1.12 (158) archive); HEAD pbxproj is `216` ×6 (`E/pbxproj-and-plists.txt`) and the plan was not updated | **holds** then; superseded (216) |
| A17 | the check-off writes exactly three fields — `done`, `doneAt`, `updatedAt` | :131-134 | code | `E/structure-checks-2.txt`: `TodayOps.setDone` sets `done`, `doneAt`, `updatedAt` and nothing else | **holds** |
| A18 | settle 320 ms after a check-off / 160 ms after an uncheck; chord hold 300 ms — the web's numbers | :135-141 | code | `WatchStore.swift:485` `done ? 320 : 160`, `:551` `.milliseconds(300)`; `app.js:1243` `delay: it.done ? 320 : 160`, `:1254` `}, 300)` (`E/structure-checks.txt`, `E/web-side-and-structure.txt`) | **holds** |
| A19 | the finale card reads "That's the list." — the web's default | :139 | code | `app.js:247 FINALE_LINE = "That's the list."`; `OneThingView.swift:64` (`E/z-last-checks.txt`) | **holds** |
| A20 | haptic table: phone `CHHapticPattern` 7 transients at 65 ms, a fuller one at 210, the chord at 700 with a 320 ms roll; Watch seven `.click` 65 ms apart then `.success` at 700 | :150-155 | code | `E/haptics-pattern.txt`: `Haptics.swift` `Volley` count 7 / step .065 / centre .210 / chord .700 / ring .320; `WatchHaptics.playFinale` 7×`.click` at `stepMs`, `.success` at `chordMs`; `fx.js:125-126` `i * 65`, 210; `packs.js:280` `t0 + 0.7` | **holds** |
| A21 | `FINALE_BUZZ` is fifteen values; check-off `buzz(8)`; uncheck no buzz; shuffle `navigator.vibrate(10)` | :166-168 | code | `E/web-side-and-structure.txt`: 15 values; `sound.js:101 buzz(8)`; `app.js:2155 vibrate(10)` | **holds** |
| A22 | `.verticalPage` is watchOS 10 and watchOS-only | :176 | SDK | `E/sdk-on-disk-2.txt`: `@available(watchOS 10.0, *)`, iOS/macOS/tvOS/visionOS unavailable | **holds** |
| A23 | the one-thing footer is the web's words: "N more after this" / "Last one" | :190 | code | `grep -nE 'more after\|Last one\|more' OneThingView.swift` → nothing; nothing under `apple/TodaysFive/` carries either string (`E/z-misc-greps.txt`); only `app.js:905` does | **doesn't hold** (the Watch has no such footer at HEAD) |
| A24 | "Shared" and "view only" pills, both can be on at once | :199-200 | code | `ListPickerView.swift:61-62` `Pill("view only")`, `Pill("Shared")`, independent `if`s; `TodayView.swift:70` | **holds** |
| A25 | exactly four accessory families; every `system*` family explicitly unavailable on watchOS | :211-212 | SDK | `E/sdk-on-disk.txt` + `E/z-misc-greps.txt`: `WidgetFamily` has 9 cases — 4 `accessory*` + 5 `system*`, each `@available(watchOS, unavailable)` | **holds** |
| A26 | `[REDACTED]` is watchOS 10 | :222 | SDK | `E/z-misc-greps.txt`: `@available(iOS 17.0, macOS 14.0, watchOS 10.0, visionOS 26.0, *)` | **holds** |
| A27 | `WKApplicationDelegate` has no URL callback; the only `openURL` in WatchKit sends a URL out | :227-228 | headers | `E/misc-checks.txt`: the protocol's 19 methods, URL/open = 0; `E/sdk-on-disk.txt`: `openSystemURL:` in `WKApplication.h`/`WKExtension.h` | **holds** |
| A28 | the extension does not link `TodaysFiveCore`; reads one small `WatchSnapshot`; Foundation only | :229-234 | pbxproj / archive | pbxproj `[REDACTED]` only on the Watch (:368) and phone (:390) targets, none on the appex (:337-351); archive appex `otool -L`: AppIntents CoreText Foundation SwiftUI UIKit WidgetKit; `strings` A86014/FF3D9A = 0 in the appex (`E/built-products.txt`) | **holds** |
| A29 | the Watch store lives in the App Group container; nil → Application Support with a debug line | :235-240 | code | `WatchStore.swift:157-166` (`E/privacy-and-structure-3.txt`); baseline prints `watch: store=appGroup`; `W/appgroup-container.txt` lists `lists/` in the group container | **holds** |
| A30 | the timeline carries now + the list's next midnight so the count is right through the night | :241-247 | reading | `watch.md` F1: `entries` replayed across midnight reverts to the stale count at 00:01 (`W/probe.log`) | **doesn't hold** (F1) |
| A31 | this round the Watch always uses `#A86014`, which Dark, Paper and Terminal all carry | :253-259 | reading | `E/z-csstext-diff.txt`: at 0e03143 `CURATED` accent = `#A86014` for exactly dark, paper, terminal; Phase 4 gave the Watch 18 kits and Paper/Terminal their own accents | **holds** then; superseded |
| A32 | Watch Keychain items `AfterFirstUnlock`, not synchronizable | :276, :461-462 | code | `KeychainLinkVault.swift:36 [REDACTED]: false`, `:75` AfterFirstUnlock (`E/privacy-prints.txt`); the file is compiled into both the phone and the Watch target (pbxproj build files 105 and 413, `E/misc-checks.txt`) | **holds** |
| A33 | payload `{ v, at, links: [{ id, mode, origin, nickname, name }] }`; unknown keys kept; unknown `v` refused | :283-299 | code / tests | `WatchLink.swift:62` the five link keys, `:104 knownKeys = [v, at, links]`, `:162-178`; `WatchLinkTests.swift:78-90` refuse `v=2`, missing, `0`, `"1"` (`E/web-side-and-structure.txt`) | **holds** |
| A34 | non-plist types refused synchronously with `[REDACTED]` — "measured" | :300-301 | probe | the constant exists (7010, `E/sdk-on-disk.txt`); the refusal not re-provoked | **can't** |
| A35 | no realtime; poll with `p_rev`; an unchanged poll costs 29 bytes | :311-313, :674 | code / live | `SupabaseTransport.swift:4` `p_rev`; `002_v3.sql:165 get_list_v3(p_id text, p_rev bigint …)`; the suites assert `< 60` bytes, not 29 (`interop.mjs:348`, `realsync4.js:102` — `E/z-misc-greps.txt`); no backend here. The quoted body `{"rev": 2, "unchanged": true}` is 29 bytes by arithmetic | **can't** (29 not pinned anywhere; would: `tfive raw` against a live list) |
| A36 | the web's recurring rollover is `setInterval(…, 60000)` | :323 | code | `app.js:2031 setInterval(() => { tickDay(); … }, 60000)` (`E/z-last-checks.txt`) | **holds** |
| A37 | `TextFieldLink` has **exactly four** initializers | :350 | SDK | `E/structure-checks-2.txt`: the 26.5 `arm64_32` swiftinterface declares 3 public inits (one generic-label, two `where Label == Text`); the same "four" at `DECISIONS:659` and `WatchDictation.swift:7` | **doesn't hold** (3 on the only SDK on disk) |
| A38 | `WKTextInputMode` has exactly three cases; `withSuggestions: nil` skips the chooser | :351-355 | headers | `E/sdk-on-disk.txt`: `Plain`, `AllowEmoji`, `AllowAnimatedEmoji` (deprecated 2.0–6.0) = 3; header comment on `[REDACTED]:` | **holds** |
| A39 | `WKApplication.shared().[REDACTED]` is public API and non-nil from a pure `WindowGroup` | :360-361, :639, :696 | one sim run | `WKApplication.h:37` declares it (`E/z-misc-greps.txt` predecessor grep in `E/sdk-on-disk-2.txt`); baseline `-TFAddSelfTest` printed `[REDACTED]=present` (the 26-char property name is struck by `safe()`). It is a **branch** — which controller will be asked — not dictation | **holds** (as a branch; the wrist question is open, Phase 5) |
| A40 | one core add: `Model.addToToday(_:text:at:)`; `Model.setDone(_:id:done:at:)` | :369, :451-452 | code | `TodayOps.swift:14 public extension Model`, `:27 addToToday(_:text:sectionId:at:idFn:)`, `:68 setDone(_:_:_:at:)`; `tfive` calls both (`TFive.swift:128,143`) | **holds** (labels differ: `setDone(doc, id, done, at:)`) |
| A41 | a free-text parameter in a phrase is a halting build error; every phrase carries `\(.applicationName)`; at most ten App Shortcuts | :392-400 | build | `TodaysFiveShortcuts.swift:35-38`: three parameterless phrases, each with `\(.applicationName)` (`E/structure-checks.txt`); the error and the limit not re-provoked (no builds) | **holds** (shape); the error and the ten **can't** |
| A42 | `.scrollInputBehavior(.enabled, for: .handGestureShortcut)` … "this app wants the activation" | :407-408 | code | `WatchApp.swift:260 .scrollInputBehavior(.disabled, for: .handGestureShortcut)` (`E/structure-checks-2.txt`) | **doesn't hold** as written (code says `.disabled`; the intent matches) |
| A43 | no developer-facing Action button API anywhere in the watchOS 26.5 SDK | :410-411 | SDK grep | not re-grepped (`E/sdk-on-disk-2.txt` searched AppIntents only) | **can't** (would: `grep -ril actionbutton` over the SDK's headers and swiftinterfaces) |
| A44 | Undo for five seconds; the Undo un-does rather than deletes | :419-420 | code / sim | `AddFlowView.swift:58 undoWindow = .seconds(5)`; baseline `add self-test: undo ok today=0 was=0 tombstoned=true` | **holds** |
| A45 | `-TFDumpWatchSend` prints shape and counts only, never a secret | :431-432 | code | `WatchLinkSender.swift:148-152`: `v links edit view shared nicknamed kits keys=` — no id, no length even; `W/handoff-phone.log` shows the line | **holds** |
| A46 | "The web app does not change. Not a byte." | :438 | git diff | `git diff --stat e215e65 4962f97 -- . ':!apple'` → empty (`E/z-misc-greps.txt`) | **holds** |
| A47 | every `print` is `#if DEBUG`; every call site passes a fixed string with counts or a length | :466 | reading | `E/privacy-prints.txt` (`E/tools/print-audit.py`): 30 app prints all `inDEBUG=True`; the 29 non-DEBUG prints are all in the `tfive` CLI | **holds** |
| A48 | screenshots named for the screen, never the list | :467 | ls | `E/files-and-shots.txt`: 29 files, all screen names or kit ids | **holds** |
| A49 | the codec was nondeterministic — `swift test` failed three runs in eight; fixed by sorting | :569-575 | swift test | not re-run (no builds); the fix is described in 7f0e112/8e3d5af | **can't** |
| A50 | `swift test` 116 tests in 9 suites — 94 + 22 | :617 | swift test | `E/test-counts-corrected.txt`: 4962f97 `@Test=116 @Suite=9`; e215e65 94/7 | **holds** |
| A51 | Node model 28, theme 31, crypto 10, sync 14, sound 12, features 28, compat 9 — all green | :618 | node | `E/node-suites-historical.txt` at 4962f97: 28/31/10/14/12/28/9, every suite exit 0 (132) | **holds** |
| A52 | destinations iOS Sim, iOS device, watchOS Sim (Debug and Release), `generic/platform=watchOS`, the archive; **zero Swift warnings in either configuration** | :619-620 | xcodebuild | not rebuilt at 4962f97. At HEAD the baseline's 5 builds + archive have 0 `warning:` lines each (`baseline.md` §5); `swift test` at HEAD prints 2 warnings in the test target (`SyncTests.swift:122-123`, `baseline.md` §2) — outside what the plan counted | **holds** at HEAD for xcodebuild; at 4962f97 **can't** |
| A53 | `3 finale: fired=yes after=0.333s` | :630 | one run | baseline: `after=0.510s` at load ~150 (`B/watch-demo-selftest.log`); Watch track ×3: 0.364 / 0.331 / 0.328 s (`W/selftest-run{1,2,3}.log`) | **holds** as a rhythm (300 ms hold + waiter granularity); 0.333 is one run's number |
| A54 | `7 finale run: taps=8/8 duration=0.707s` | :634 | one run | baseline `0.707s`; Watch track 0.713 / 0.710 / 0.709 s | **holds** (eight calls scheduled; what a wrist feels is not measured) |
| A55 | `5 shuffle x10: distinct=5 sameTwiceInARow=0` | :631, :642-644 | one run | baseline 5 / 0; Watch track 5 / 4 / 5 and 0 / 0 / 0 | **holds** for the rule (never twice in a row); `distinct=5` is one run's luck (run 2 drew 4) |
| A56 | `9 snapshot: hasList=true done=4/5 nextLine=19 chars viewOnly=false rollsAt=set` | :636 | one run | baseline: identical line plus `kit=terminal pair=mono face=IBMPlexMono-SemiBold` | **holds** |
| A57 | add self-test 5 of 5 | :638 | one run | baseline `end pass=5/5` (`B/watch-add-selftest.log`) | **holds** |
| A58 | hand-off log `sent v=1 links=1 … heard v=1 links=1 applied=true +1 −0 holds=1` | :650-653 | sim pair | Watch track on the review pair: `sent v=1 links=0 … / heard v=1 links=0 applied=true +0 −0 holds=0` — the phone held no list (`W/handoff-*.log`) | **holds** (channel and stamp); the `links=1` line **can't** without a vaulted link |
| A59 | removal reaches the wrist; a second delivery answers `applied=false +0 −0` | :656-659 | sim pair | not reproduced (needs a link in the phone's vault); the reconciler rule is in `WatchLinkTests` | **can't** |
| A60 | live run: rev 6 → 13; 29 bytes vs 621; the Shared pill; deleted and gone | :663-676 | live backend | no backend here | **can't** (would: the same run with `tfive` and the interop harness) |
| A61 | screenshots `today`, `today-after-selftest`, `empty`, `from-the-phone`, `live-backend` exist | :680-684 | ls | `E/files-and-shots.txt`: all five present | **holds** |
| A62 | one-thing, the picker and the complication faces are not photographed | :686-689 | — | at HEAD `theme-picker.png` and `count-actions.png` were added in Phase 4; still no one-thing page and no complication (29 files) | **holds** (still true for one-thing and the faces) |

## B. `apple/PLAN-apple-phase4.md`

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| B1 | Xcode 26.6 (17F113), Swift 6.3.3; watchOS 11.1 (22R581) / 26.5 (23T570) | :22-23 | env | `E/environment-and-archives.txt` | **holds** |
| B2 | pair `4594CB69` + `C33542F5` = `4B6A97F5`, active and connected | :24 | simctl | `E/misc-checks.txt` | **holds** |
| B3 | `git rev-list --count main` = 169 | :25 | git | `git rev-list --count 4962f97` = 169 (`E/z-commit-counts.txt`) | **holds** |
| B4 | the web is 1.12 (158); deployed `version.js` and `sw.js` match the checkout byte for byte | :26 | curl / cmp | the deployed site was not fetched | **can't** (would: `curl` both files and `cmp` against `git show 4962f97:…`) |
| B5 | Node 132 tests (28/31/10/14/12/28/9) | :27 | node | `E/node-suites-historical.txt` at 4962f97 = 132 | **holds** |
| B6 | `swift test` 116 tests in 9 suites, 8.5 s | :27 | swift test | 116/9 ✓ (`E/test-counts-corrected.txt`); the 8.5 s not re-run | **holds** (count); the time **can't** |
| B7 | fontTools 4.60.2 + brotli via `pip install --user` | :29 | pip | `E/environment-and-archives.txt`: fontTools 4.60.2, brotli 1.2.0 | **holds** |
| B8 | 26 faces, 732 KB of woff2 → **1.54 MB** of TTF | :35-36 | fontTools | `E/fonts.txt`: 26 woff2 = 732,924 B; straight 26-file conversion = 1,610,380 B = 1.61 MB decimal = **1.54 MiB** | **holds** in MiB (labelled MB) |
| B9 | Google's own TTFs would be 6.23 MB | :37 | download | not fetched | **can't** |
| B10 | a hand-assembled `UIAppFonts` app rendered a bundled face on 11.1 and 26.5 | :37-39 | sim | not re-run; baseline `-TFFontSelfTest` on 26.5: `bundled=33/33 … pass=96/96` | **holds** on 26.5; 11.1 **can't** |
| B11 | watchOS loads `.woff2` — `libFontParser.dylib` carries `WOFF2Container` | :39-40 | strings | `strings libFontParser.dylib | grep -c WOFF2Container` → 1 in the watchOS 26.5 simulator runtime (`E/z-last-checks.txt`) | **holds** (the symbol; loading not tested) |
| B12 | `styles.css` has 26 `@font-face` rules | :43 | grep | `E/fonts.txt`: 26 | **holds** |
| B13 | `outfit-500-800.woff2` is "Outfit Thin", `manrope-500-800` "Manrope ExtraLight", `dm-sans-400-700` "DM Sans 9pt" | :44-45 | fontTools | `E/z-woff2-family-names.txt` nameID 1: exactly those three strings | **holds** |
| B14 | **10 of 22** families resolve to Helvetica | :47, :460 | CoreText | not re-run (needs a CoreText process registering the 26 files). `Fonts/README.md:15` and `Kits.swift:99` say **twelve**; 9a2f369 explains the two extra (Lato at 900, IBM Plex Mono at 600) — the two counts measure different things and neither document says which | **can't**; inconsistent (10 vs 12) across the write-ups |
| B15 | Outfit has 9 `fvar` instances; Fraunces 0; source-serif-4 0 | :53-55 | fontTools | `E/fonts.txt`: outfit namedInstances 9; fraunces 0; source-serif-4 0 | **holds** (the ink fractions **can't**) |
| B16 | `\.colorScheme` reads `.dark` at the root; `.preferredColorScheme(.light)` inert; `.environment` works | :59-62 | sim | not re-run | **can't** (would: a sim launch printing `\.colorScheme` at the root) |
| B17 | `WidgetRenderingMode.accented` "treats the widget's views as if they were template images…"; `fullColor` "Only applies to iOS" | :67-70 | SDK | `E/sdk-on-disk-2.txt`: both strings in the watchOS WidgetKit `.swiftdoc` | **holds** |
| B18 | `sync.js:331` is the only broadcast; `SyncEngine.push()` lacked it; `put_list_v3` (`002_v3.sql:195-243`) has no trigger/`realtime.send`/`pg_notify`; `grep supabase/` → 0; joining moves the poll 60 s → 240 s | :79-84 | grep | `E/web-side-and-structure.txt`: `sync.js:331` send, `POLL_LIVE_MS = 240000`, `POLL_MS = 60000`, supabase grep 0 hits; `put_list_v3` at :195, its `$$;` at :243; `SyncEngine.swift:215` now rings | **holds** |
| B19 | latency medians **54 s** (connected) / **32 s** (not); backgrounded never 10/10 | :87-91 | `polld.js`, ten trials | not re-run (Playwright + load); the round's own re-runs gave 132 s and 144 s (fb018ce, `DECISIONS:1929`); the checkpoint table stands uncorrected in place | **doesn't hold** as a number (single-run median); the ratio/shape **can't** |
| B20 | JavaScriptCore in the macOS and iOS SDKs, absent from watchOS; `CURATED` byte-identical, **17,674 bytes**, ~6 ms | :96-99 | SDK / JSContext | `E/sdk-on-disk.txt`: watchOS NO / iOS yes / macOS yes; `E/structure-checks.txt`: `JSON.stringify(CURATED).length` = 17674 at 0f11ee6; 17676 at HEAD (`E/kits-tokens-2.txt`); baseline KitFixtureTests' drift test ✔ on macOS | **holds** (17,674 then, 17,676 now); 6 ms **can't** |
| B21 | 4.5:1 on Paper needs L ≤ 0.159, on Terminal ≥ 0.188; `#A86014` clears all four grounds by 16 % | :108-111 | arithmetic | `E/z-contrast-arithmetic.txt`: ≤ 0.1591 / ≥ 0.1876; `#A86014` vs ink3 = 3.476 / 3.479 = 1.16× the 3:1 floor | **holds** |
| B22 | only three kits carry `#A86014` — dark, paper, terminal | :118, :429 | cssText | `E/z-csstext-diff.txt`: at 0e03143 exactly `dark,paper,terminal`; at HEAD `dark` | **holds** |
| B23 | terminal 4.12 → 13.29 (ink) / 3.48 → 11.22 (ink-3); paper 4.33 → 4.79 / 3.48 → 3.85; dark 4.12 / 3.48 | :124-126, :434-436 | theme.test.js | `E/z-contrast-arithmetic.txt`: 13.2924 / 11.2212, 4.7867 / 3.8477, 4.1178 / 3.4762, `#A86014` on paper 4.3284 / 3.4793 | **holds** |
| B24 | `finalize()` leaves all six reverted hexes untouched (`fix3` run on each) | :129-131, :438-440 | fix3 by hand | not re-run | **can't** (would: import `theme.js`, run its nudge on the six) |
| B25 | light's accent 3.0016 on ink-3; pink's 4.0144; pink's accent text clears by 0.0069 | :151-154, :446-448 | theme.test.js | `E/z-contrast-arithmetic.txt`: 3.0016; 4.0144; pink accentText/ink3 4.5069 | **holds** |
| B26 | `derive()` worst accent vs ink3 **2.18** on dark (**1,152** of 3,000), **2.45** light (**1,687**) | :155-158 | 3,000 seeded accents | not re-run; `DECISIONS:962-963` reports **1,139** and **1,690** for the same measurement | **can't**; inconsistent within the round (1,152 vs 1,139; 1,687 vs 1,690) |
| B27 | `light.danger` `#B8402A` on `light.ink3` = 4.12 | :162-164 | contrast | 4.1205 (`E/z-contrast-arithmetic.txt`) | **holds** |
| B28 | `panels.js` `#sw-yours` fill had no `dev.secret` guard — "Closed." | :168-172 | reading | `DECISIONS:1024`: gated inside `savedThemes()` instead; `panels.js:136 fill("#sw-yours", saved.map(…))` — the guard's placement not read here | **can't** cheaply (would: read `savedThemes()` and the three-door test in `theme.test.js`) |
| B29 | `WebViewController.swift:57` paints `#1A1D21` behind the web view — stale since 1.11 — "**Fixed.**" | :173-175 | reading | `E/webviewcontroller-colour.txt`: HEAD `:61 view.backgroundColor = UIColor(red: 0x1A/255, green: 0x1D/255, blue: 0x21/255, …)`; `LaunchBackground.colorset` is `#070A08`; no commit in the range touched the line | **doesn't hold** ("Fixed" is not fixed) |
| B30 | `mark.mjs --trace` "should still report **1.60 %** — verified, not assumed" | :176-179 | mark.mjs | `E/mark-trace.txt`: 0.30 % at HEAD; `DECISIONS:1038-1045`: it reported **93.84 %** at the time; `README:275-276`: 1.60 % was the pre-1.11 number | **doesn't hold** (contradicted by the round's own record) |
| B31 | the render pass takes ~20 s | :186-187 | kitshots.js | not re-run (Playwright) | **can't** |
| B32 | **62 of the 314** hex tokens absent from `theme.js`'s source; every kit has ≥ 1; Harbor `#046D6D`, Teletype `#119449`, Sketch `#9D7700`, Dark's greys, every `hairSolid` | :195-198 | kit-tokens | `E/kits-tokens-2.txt` (`E/tools/kit-tokens.mjs`): 62/314 at 0f11ee6 and 9a2f369; **58/314 at HEAD**; 18/18 kits; the three named tokens absent; `hairSolid` 18/18; `E/z-contrast-arithmetic.txt`: dark's muted/dim/done/muted2/dim2/done2/hairSolid absent, text/ink2/ink3 present | **holds** then; 58 now (`README:45`, `Kits.swift:9`, `KitsGen.swift:5` still say 62) |
| B33 | a 14-iteration gamut binary search and a 60-step lightness loop | :199 | reading | `theme.js:54 i < 14`, `:75 i < 60` (`E/z-misc-greps.txt`) | **holds** |
| B34 | `/usr/bin/env -i /bin/sh -c 'command -v node'` exits 1 | :202 | shell | `E/environment-and-archives.txt`: `env -i node: exit=1` | **holds** |
| B35 | `gen-kits.mjs` refuses to write; `KitsGen` emits the 16 open kits only; `superpink`/`birthday` in neither the table nor any binary | :209-211, :219, :452-453 | assertions / strings | `E/kits-tokens.txt` asserts (18 kits, 2 secret, 13 pairs, floors); `E/built-products.txt`: `Kit(` literals 16; `superpink`/`birthday`/`FF2E9A`/`D62E86` = 0 in every binary, dylib and object | **holds** |
| B36 | `KitFixtureTests`: `#if canImport(JavaScriptCore)` drift test against live `theme.js`; floors in Swift on all 18 | :215-221 | swift test | `E/test-counts.txt` guard at :22/:37; `baseline.md` §2: six ✔ lines incl. "live theme.js still produces the fixture" and "every kit clears its own grounds — all 18" | **holds** |
| B37 | a nested kit object survives encode→decode, payload **394 bytes**; keys must be inserted sorted | :228-231 | swift test | `WatchLinkTests.swift:311-323` measures and **prints** the byte count, pins nothing (`E/web-side-and-structure.txt`); 7f0e112 quotes 1,178 B for both kits + a link; no `swift test` here | **can't** (would: `swift test` and read the print) |
| B38 | the picker stores in `UserDefaults(suiteName: "group…")`; the selected-list key is in `.standard` because it is a secret | :249-251 | code | `WatchTheme.swift:381` suiteName; `WatchLinkReceiver.swift:66,74` (`E/web-side-and-structure.txt`, `E/stale-numbers-2.txt`) | **holds** |
| B39 | **twelve** `.primary`/`.secondary` call sites are only right because watchOS is dark | :255 | reading | `E/stale-numbers-2.txt` at 4ca4cb0^: **14** call sites in 5 view files (+2 constants in `WatchTheme`, +1 in the appex); 4ca4cb0 removed 16 lines | **doesn't hold** (14; `DECISIONS:1310` has it right) |
| B40 | the system clock stays white on cream; no API changes it | :256-257 | — | `WatchTheme.swift:645-680` `ClockScrim` (ed89bd7) now patches under it; the "no API" not re-tested | **holds** (mitigated since) |
| B41 | dark **0.0466**, cream **0.8146**, **17.5×**, stable 17.2–17.8× across four weightings | :258-264 | two screenshots | superseded by a4f180a: 0.0285 / 0.7868 / 27.5–27.6×; `DECISIONS:1367-1375` says the denominator is fragile and the ratio unstable | **doesn't hold** (the round's own re-measurement) |
| B42 | `Canvas` and `TimelineView(.animation)` are watchOS 8.0+; **180** rotating particles | :265-266 | SDK / sim | `E/z-last-checks.txt`: `Canvas` `@available(… watchOS 8.0, *)` (SwiftUICore); `TimelineView` watchOS 8.0 (`E/sdk-on-disk.txt`); the field is 222 (7×26+40) and :467 says 222 | **holds** (availability); 180 is stale |
| B43 | `[REDACTED].entries(from:mode:)` returns zero entries in `.lowFrequency` on both runtimes | :267-268 | sim | not re-run | **can't** |
| B44 | `WatchSnapshot` grows fields with `v` left at 1; `publish()` writes and calls `reloadAllTimelines()` | :271-274 | code | `WatchSnapshot.swift:45 version = 1`, defaults at :89-100; `WatchStore.swift:610 reloadAllTimelines()`; baseline `face: … snapshot written, reloadAllTimelines requested` | **holds** (a reload *request*; not a redraw — `watch.md` F6) |
| B45 | fonts must be duplicated into the appex: 1.5 MB and 26 hand edits | :275-277 | plan | superseded: the appex carries 0 `.ttf` and reads the app's (`DECISIONS:1681-1699`; archive appex ttf = 0, `W/fonts-check.txt`) | **holds** then; superseded |
| B46 | screenshots of all 18 kits, named by id | :278, :468 | ls | `E/files-and-shots.txt`: 18 `today-<id>.png` | **holds** |
| B47 | the REST broadcast fallback (`{ apikey, Content-Type }`, no Bearer) answers … | :287-292 | curl | `DECISIONS:1915` HTTP 202; no backend here | **can't** |
| B48 | ~138 bytes per write, zero at idle; the 29-byte poll unchanged | :300-301, :495 | SyncTests | `SyncTests.swift:433,437` pin 137 / 138 (`E/web-side-and-structure.txt`); `rawGet` untouched per `DECISIONS:1902`; 29 not measurable here | **holds** (138); 29 **can't** |
| B49 | shortening `POLL_LIVE_MS` = 4× the polls at idle; backgrounded 10/10 never | :305-307 | arithmetic / polld | 240000/60000 = 4; polld not re-run | **holds** (4×); 10/10 **can't** |
| B50 | `polld.js`: `page.clock.install`, a random phase offset, `page.clock.runFor`; 40 trials in ~six minutes | :312-315 | reading | `polld.js:22-25,101,140,184` (`E/z-misc-greps.txt`); the six minutes not re-run | **holds** (mechanism); the time **can't** |
| B51 | §5 line refs: `features.test.js:243` and `:296` the two array copies; `:225` headline; `e2e4.js:1265/:1637` arrays; `:1248/:1674/:2175` toast; nth-child(5) at `:1638/:1639`; `features.test.js:233` the wink | :328-335, :357, :362-363 | reading | checked at HEAD: `:243` pins the array; `:225` headline regex; `:233` pins 1.8's headline; `e2e4.js:1265`/`:1637` arrays; `:1638-1639` `nth-child(5)`; `:1248`/`:1674`/`:2175` the toast — but `features.test.js:296` is the `lang="en"` assertion, not a versions copy | **holds** except `:296` (points at a different assertion) |
| B52 | the release step: "four build homes" (then lists five) plus the pbxproj's **six** `CURRENT_PROJECT_VERSION`; `MARKETING_VERSION` 1.12 on all six; `CACHE = VERSION + "-b" + BUILD` | :340-345 | grep | `E/archives-and-versions.txt`: `sw.js` `VERSION "tf-v1.12"`, `BUILD = 216`, `CACHE = VERSION + "-b" + BUILD`; `data-build="216"`; `PANELS_BUILD = 216`; whatsnew `"build": 216`; `E/z-pbxproj-ids.txt`: `CURRENT_PROJECT_VERSION = 216` ×6, `MARKETING_VERSION = 1.12` ×6, no other value | **holds** (the numbers); the sentence miscounts (says four, lists five) |
| B53 | about thirty "1.12 b216" code comments | :347 | grep | `E/stale-numbers.txt`: 40 lines incl. markdown; `E/structure-checks.txt`: 34 non-markdown | **holds** (≈) |
| B54 | no 1.6 in `whatsnew.json` or `CHANGELOG.md`; the Secret pair shipped as 1.6 (`12e62e3`, `version.js` "1.6" at build 76); the wink now at 1.8, pinned at `features.test.js:233` | :352-358 | git | `E/archives-and-versions.txt`: versions 1.12…1.7, 1.5 (no 1.6), CHANGELOG headings likewise; `12e62e3:version.js` `VERSION = "1.6"`; `features.test.js:233` pins 1.8's headline; `whatsnew.json:78` "If you know, you know." — the build-76 figure not checked | **holds** |
| B55 | pbxproj ids are `TF` + nineteen `0`s + 3 digits; free bands 264–299 (`PBXFileReference`) and 436+ (`PBXBuildFile`) | :375-376 | reading | `E/z-pbxproj-ids.txt`: 177 distinct `TF0{19}NNN` ids, 176 definition lines, 0 duplicates, no other id shape; the bands are now occupied (264–297 and 436–469, the 33 fonts + `OFL.txt`; `E/pbxproj-and-plists.txt`) | **holds** (free then; used now) |
| B56 | `e2e4.js` ~25 minutes; **169 passed, 0 failed**; zero page errors / CSP / third-party | :393-394, :423 | e2e4 | `baseline.md` §4 run 2: `169 passed, 0 failed`, 1117 s (18 min 37 s), the two "no page errors, CSP violations or third-party requests" lines ok at both viewports | **holds** (25 min → 18.6 min here) |
| B57 | Node **136** tests — model 28, theme 33, crypto 10, sync 14, sound 12, **features 29**, compat 9 | :421 | node | the seven listed sum to **135**; `baseline.md` §1 at HEAD: 28/33/10/14/12/**30**/9 = 136 | **doesn't hold** (the list is 8a412a3's 135; the total is right by a later commit) |
| B58 | `swift test` **130 in 10 suites**, 8.3 s (116 before) | :422 | swift test | `baseline.md` §2: "130 tests in 10 suites passed after 8.389 seconds"; `E/test-counts-corrected.txt` 130/10 | **holds** |
| B59 | iOS Simulator and watchOS Simulator builds, zero warnings in either | :424 | xcodebuild | baseline 5a/5b (and 5c/5d/5e): 0 `warning:` lines | **holds** (and wider: five destinations + archive) |
| B60 | `-TFFontSelfTest` **98/98**, `-TFConfettiSelfTest` **108/108** | :425 | sim | baseline: font `end pass=96/96` (`kits=16`), confetti `end pass=96/96` (16 kits × 6 checks) — `B/watch-font-selftest.log`, `B/watch-confetti-selftest.log`; 98 = 96 + 2 kits, 108 = 18 × 6: those runs had the Secret pair unlocked on that simulator, which the write-up does not say | **doesn't hold** as printed here (a state-dependent denominator) |
| B61 | the `cssText()` diff moves **exactly two** kits; fifteen are byte-identical | :430, :548-549 | cssText | `E/z-csstext-diff.txt` (`node`, `theme.js` at 0e03143 vs HEAD): 15 identical; **3 moved** — light (`danger`), paper, terminal | **doesn't hold** (three; :559 itself says light's danger moves); the fifteen **holds** |
| B62 | the mark did not move; `--trace` confirms | :442-444 | mark.mjs | `E/mark-trace.txt`: `--check` unchanged, `--trace` 0.30 % | **holds** |
| B63 | **33 static faces, 1.3 MB** | :458 | ls | 33 `.ttf` = 1,238,128 B = 1.24 MB (1.18 MiB); `du -sk` of the directory with `OFL.txt` + `README.md` = 1300 KiB (`baseline.md`) | **holds** loosely (1.24 MB of fonts; "1.3" is the directory in KiB) |
| B64 | the picker persists across a cold launch | :465-466 | `-TFThemeSet`, two launches | not re-run | **can't** (would: the two launches on the review pair) |
| B65 | 222 particles holding ~52 fps | :467 | sim | baseline `10 confetti: run=1 frames=134 over 2.56s = 52.0/s`; Watch track 54.5 / 51.8 / 52.0 | **holds** (on this simulator) |
| B66 | dark **0.0285**, cream **0.7868**, **27.5–27.6×** | :470-473 | screenshots | not re-measured; consistent with `DECISIONS:1344-1348` | **can't** (would: two `-TFKit` shots at 416×496 and the drive script, which is uncommitted) |
| B67 | the clock over Paper measures 1.12:1 | :473 | pixels | `E/kits-tokens-2.txt`: white vs `#F7F2E8` = 1.12 (1.116) | **holds** |
| B68 | `.accented` and `fullColor` renderings byte-identical | :475-478 | `-TFFaceProbe` | `E/baseline-selftests-2.txt`: `watch-faceprobe.log: not present`; not run | **can't** |
| B69 | three runs 54 / 132 / 144 s; the 4× ratio; backgrounded never 10/10 | :487-491 | polld | not re-run; `SyncEngine.swift:258` carries "a write took a median of 132 s" — one run's median in a code comment | **can't** (single runs; the code comment repeats one) |
| B70 | `derive()`'s fix pulled onto `derive-ink3-next` | :500 | git | branch `derive-ink3-next` and `origin/derive-ink3-next` present (`E/z-misc-greps.txt`) | **holds** |
| B71 | Surprise me over 2,000 themes moved 0; four `T2:` codes moved 0; the ~47 % came from uniform-random hex | :505-507 | Ink track | Ink track's claim — listed here only; (1152 + 1687) / 6000 = 47.3 % | **can't** (Ink's); the 47 % arithmetic **holds** |
| B72 | live run: put **0.52 s**; line on the page **0.63 s**; 240 s before; 29 bytes vs 593; envelope 565; 0 page errors | :519-527 | an ad-hoc script | 5cd559f touches only `PLAN-apple-phase4.md` (+28 lines); nothing in the tree prints `live=true` or the figure (`E/stale-numbers-2.txt`); no backend | **can't** (uncommitted instrument) |
| B73 | three lists created, all confirmed gone | :533 | live | no backend | **can't** |
| B74 | `--danger` `#B8402A` → `#B13924`, at most 7/255 per channel, 4.12 → 4.51 | :559-561 | arithmetic | deltas 7, 7, 6; 4.1205 → 4.5050 (`E/z-contrast-arithmetic.txt`) | **holds** |
| B75 | a returning b158 device paints `#4AF07A` / `#C8321F`, caches stamped `1ptwfkh`; five configurations | :592-609 | Playwright vs the deployed site | `index.html:2 data-tokens-rev="1ptwfkh"`, `:63` the boot check (`E/z-misc-greps.txt` predecessor grep); the runs not reproduced | **can't** (the stamp is present) |

## C. `apple/DECISIONS-apple.md` — the in-range sections (1.12 finale :455-513, Phase 3 :514-888, Phase 4 :889-1994; 0e03143's copy ended at line 451)

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| C1 | the pattern: 7 transients at 0.065 steps (0.55/0.45), one at 0.210 (0.80/0.30), the chord at 0.700 (1.00/0.25), continuous 0.700–1.020 (0.45/0.15); total 1.020 s | :462-469 | Haptics.swift | `E/haptics-pattern.txt` + `E/z-last-checks.txt` `Haptics.swift:98-106`: `tap(0.55, 0.45)` ×7 at `i * step`, `tap(0.80, 0.30)` at centre, `tap(1.00, 0.25)` at chord, continuous 0.45/0.15 from chord for `ring`; 0.700 + 0.320 = 1.020 | **holds** |
| C2 | `-TFSelfTest` prints `finale pattern ok duration=1.020s hardware=no` | :469, :503-506 | phone sim | no phone self-test in the baseline (`phone-selftest.log: not present`); `Haptics.swift:137` prints `p.duration` — the sum of its own constants | **can't** (not run); a one-answer print — it reports the pattern's own arithmetic |
| C3 | the project links no system framework explicitly | :482-483 | pbxproj | `E/pbxproj-and-plists.txt`: framework lines 0 | **holds** |
| C4 | the prebuild command writes about two hundred bytes | :542 | ls | `Config.generated.swift` = 271 B (`E/baseline-state-and-generated.txt`) | **holds** loosely (271) |
| C5 | App Groups needed the account; the group is in the signed entitlements | :548-567 | codesign | `E/entitlements.txt` | **holds** |
| C6 | CoreHaptics not in the watchOS SDK; `WKInterfaceDevice.play(_:)` takes a type only | :571-574 | SDK | `E/sdk-on-disk.txt`; archive Watch binary links no CoreHaptics | **holds** |
| C7 | 262144 / 65536 / 65536, identical in both runtimes | :609-611 | on disk | `W/wc-payload-size-limits.txt` (watchOS runtime) | **holds** (watchOS; iOS half not re-read) |
| C8 | `transferUserInfo` did not deliver at all on this pair | :612-614 | probe | not re-run | **can't** |
| C9 | phrase rules (AppEntity/AppEnum only; `\(.applicationName)` required; same target; ten App Shortcuts max) | :622-635 | build errors | shape holds (three phrases with `\(.applicationName)`, one shared file in both targets — `E/structure-checks.txt`, `E/misc-checks.txt`); the errors and the limit not re-provoked | **holds** (shape); errors/limit **can't** |
| C10 | `simctl` cannot tap a watch simulator; Keychain entitlements enforced on the watch simulator | :653-655 | — | every instrument in this review was built around the first (`watch.md`); simulator bundles carry entitlements in `__TEXT,__entitlements` (`E/privacy-and-structure-3.txt`) | **can't** (consistent) |
| C11 | `TextFieldLink` has four initializers; `WKTextInputMode` has three cases | :659 | SDK | 3 inits (`E/structure-checks-2.txt`); 3 cases (`E/sdk-on-disk.txt`) | **doesn't hold** (inits); the cases hold |
| C12 | no Action button API in the 26.5 SDK; no Ultra hardware in the simulator runtimes | :680-681 | SDK | not re-grepped | **can't** |
| C13 | `#A86014` is what Dark, Paper and Terminal all carry | :703 | theme.js | `E/z-csstext-diff.txt` at 0e03143: yes | **holds** then; superseded |
| C14 | the web's rollover is `setInterval(…, 60000)` | :713 | code | `app.js:2031` | **holds** |
| C15 | the complication holds a snapshot — name, done, total, next line, stamp; Foundation only; no package dependency | :730-731 | code | appex has no `[REDACTED]`; `W/appgroup-container.txt` `snapshot.json` (210 B) = `{at, done, face, hasList, kit, name, next, pair, rollsAt, total, v, viewOnly}` | **holds** |
| C16 | the face is right through the night with nothing running | :741-743 | reading | `watch.md` F1 | **doesn't hold** |
| C17 | the codec failed three runs in eight; sorted now | :756-770 | swift test | no `swift test` here | **can't** |
| C18 | `sendVault` refuses to speak on a failed read; the receiver checks its writes before the mark | :772-787 | code | `WatchLinkReceiver.swift:139` "vault refused the write, mark not set" exists (`E/privacy-prints.txt`); `WatchLinkSender.swift:108` "vault unreadable, sending nothing" | **holds** (by reading) |
| C19 | the stamp is `max(now, lastSentAt + 1)`, monotonic per process; the mark persistent; a fast clock poisons the channel | :789-799 | reading | `watch.md` F4 quantified it with `tfprobe` (`W/probe.log`): outage = the clock error, to the millisecond | **holds** (and F4 shows it is worse than written) |
| C20 | `tfive check` now refuses a View link | :801-807 | code | not confirmed by grep (`E/z-misc-greps.txt` found no matching line in `TFive.swift` with the pattern tried) | **can't** cheaply (would: read `TFive.swift`'s `check` command) |
| C21 | `addToToday` does stripBidi → trim → collapse → cut at 200 | :811 | code | `TodayOps.swift:30 collapseSpaces(trim(stripBidi(…))).prefix(textMax)` (`E/structure-checks-2.txt`) | **holds** |
| C22 | 24 identical appex crashes; the `[REDACTED]` "fix"; the control run showed it was not; `NSExtension` alone is the shape | :821-866 | unified log | HEAD `ComplicationsInfo.plist`: `NSExtension` → `com.apple.widgetkit-extension`; `[REDACTED]` 0 hits (`E/z-last-checks.txt`); the crashes not re-provoked | **holds** (HEAD shape); the control **can't** |
| C23 | `-TFAddSelfTest` moved off the sheet to the app's root view | :871-878 | run | `AddFlowView.swift:251-258` `AddSelfTest.runIfAsked()` called from `WatchApp.swift:230`; baseline printed 12 lines incl. `end pass=5/5` | **holds** |
| C24 | `kitshots.js`: 18 kits in 9.4 s, zero page errors | :905-906 | Playwright | not re-run | **can't** |
| C25 | luminance bounds; fifteen of eighteen never shared the accent; 4.12→13.29 etc.; **cssText moved on exactly two** | :922-932 | theme.test.js | as B21/B23/B61; `E/z-csstext-diff.txt`: three moved | **doesn't hold** (exactly two); the numbers hold |
| C26 | `derive()` before: 2.18 (`#11735B`) **1,139**/3,000; 2.45 (`#DA6D7F`) **1,690**/3,000; after 0/3,000 | :960-964 | 3,000 seeds | not re-run; `PLAN:157-158` says 1,152 and 1,687 for the same measurement | **can't**; inconsistent with the PLAN |
| C27 | `light.danger` 4.12 on ink3 `#E4DED2`, 5.20 on ink; `#B13924`: 4.1205 → 4.5050 (ink3), 4.6907 → 5.1284 (ink2), 5.2034 → 5.6889 (ink) | :987-994 | contrast | `E/z-contrast-arithmetic.txt`: 4.1205 → 4.5050; 5.2034 on ink (ink2 not computed) | **holds** |
| C28 | light 3.0016 on ink-3; Pink's accent text 4.5069 | :1005 | theme.test.js | 3.0016 / 4.5069 computed | **holds** |
| C29 | `savedThemes()` gate: before 2 swatches in Yours, after 1 | :1013-1027 | browser | not re-run | **can't** |
| C30 | `--trace` reports 93.84 %, identical on the base commit; `--check`: worst painted radius 0.316 of 0.400, ink 6.6 % | :1044-1051 | mark.mjs | `E/mark-trace.txt`: `--check` "worst painted radius 0.316 of 0.400; ink covers 6.6 %"; `--trace` now 0.30 % (fixed at 295134a per README) | **holds** (`--check`); 93.84 superseded |
| C31 | the Secret key is in plaintext in `tools/shots.js` | :1061-1069 | reading | not read here (Ink track's surface) | **can't** cheaply (would: read the `#c-import` fill in `tools/shots.js`) |
| C32 | 62 of 314; all 18 kits; every `hairSolid`; 14-iteration; 60-step | :1078-1083 | kit-tokens | as B32/B33 | **holds** then (58 now) |
| C33 | `env -i` exits 1 | :1090 | shell | as B34 | **holds** |
| C34 | JSContext byte-identical 17,674 bytes, ~6 ms; JavaScriptCore absent from watchOS; guarded `#if canImport` | :1104-1112 | swift test | as B20 | **holds** (17,674 then) |
| C35 | a second prebuild plugin on one target: both builds succeed | :1129-1139 | xcodebuild | baseline 5a/5b succeeded with both plugins (`baseline.md` §5) | **holds** |
| C36 | 10 of 22 → Helvetica, the named ten; Nunito is "Nunito Sans 12pt ExtraLight 12pt" | :1148-1157 | CoreText | the Nunito name is in `Fonts/README.md:28` (read back from the file); the resolution count **can't** be re-run; twelve elsewhere | **can't** (10 vs 12 unresolved) |
| C37 | 33 static faces, 1.3 MB, from 732 KB of woff2 | :1169 | ls | 33; 1,238,128 B; instanced from **24** files = 704,776 B (688 KiB) — 732 KB is all 26 (`E/fonts.txt`, `E/archives-and-versions.txt`, `Fonts/README.md:44`) | **holds** loosely (24 files / 688 KiB actually used) |
| C38 | the generated table holds 16; the Secret pair in no binary; asserted by `KitFixtureTests` | :1178-1180 | strings | as B35; baseline ✔ "neither Secret kit is in the table, and neither is anywhere in this binary" | **holds** |
| C39 | keys inserted sorted; a test says so | :1190-1193 | swift test | not re-run | **can't** |
| C40 | "measured … against `Kits`' generated table at build 158" | :1204, :1393 | — | the builds were 158 then; HEAD is 216 | **holds** then |
| C41 | only Dark carries `#A86014` now | :1217 | theme.js | `E/z-csstext-diff.txt` HEAD: `dark` | **holds** |
| C42 | 17pt relative to `.title3` renders at **16.00**; Lato's −0.025em is **−0.400 pt**; Lato Black at 16.00 stands **19.20** tall vs a 18.24 target; extra leading 0.00 | :1255-1264, :1282-1283 | one sim | baseline `-TFFontSelfTest` on a fresh 26.5 sim: "at 17pt title3 scales to **17.00**, task line height **20.40**, tracking **−0.425**, extra leading 0.00" (`B/watch-font-selftest.log`); `E/fonts.txt`: Lato Black hhea at 16pt = 19.2 → 20.4 at 17pt, so both printouts agree with the font; the 16.00 was that simulator's text-size setting | **doesn't hold** as printed (the "one number that is not the obvious one" was a device setting); the rule (floor at zero) holds |
| C43 | font tally: `kits=16 pairs=13 files=33 familiesOnDevice=75`, `bundled=33/33`, `advance=12 ink=1`, `pass=96/96` | :1278-1281 | sim | baseline: the identical four lines | **holds** |
| C44 | IBM Plex Mono Regular and SemiBold set the same 21-char ruler to 214.20 pt | :1287-1288 | CoreText | `E/fonts.txt` (fontTools): advance 600/1000 in both → 21 × 17 × 0.6 = 214.2 | **holds** |
| C45 | the 16 open kits name only 11 of the 13 pairs; `baloo`/`fredoka` Secret-only; 74 → 96 checks | :1294-1300 | reading | `E/kits-tokens-2.txt`: 11 of 13, unnamed `fredoka,baloo`; 96 ✓ baseline | **holds** |
| C46 | fourteen call sites across five files | :1310 | git | `E/stale-numbers-2.txt` at 4ca4cb0^: 14 in `AddFlowView`, `ListPickerView`, `OneThingView`, `TodayView`, `WatchApp` | **holds** |
| C47 | platters are the kit's `ink2`; Dark `#0E140F` on `#070A08` | :1317-1318 | theme.js | `theme.js:145 TERMINAL_GROUND ink2 "#0E140F"` | **holds** |
| C48 | clock glyph (255,255,255) on (247,242,232) = 1.12:1 | :1322-1323 | pixels | 1.116 from the hexes (`E/z-csstext-diff.txt`) | **holds** |
| C49 | drive table dark 0.0285 / terminal 0.0284 / paper 0.7868; 27.5–27.6×; 51.1 % of 206,336 px the ground | :1342-1354 | screenshots | 416 × 496 = 206,336; 105,390 / 206,336 = 51.08 %; the drives not re-measured | **holds** (arithmetic); the drives **can't** |
| C50 | prior 0.0466 / 0.8146 / 17.5×; the cream agrees within 3.5 % | :1367-1368 | — | 0.8146 / 0.7868 = 1.035 | **holds** (arithmetic) |
| C51 | three keys under `tf/app/watch/kit/` in the App Group; the selected-list key in `.standard` | :1450-1451 | code | `WatchTheme.swift:337-339`; `WatchLinkReceiver.swift:66` | **holds** |
| C52 | persistence verified across a cold launch (`day:harbor`) | :1456-1463 | two launches | not re-run | **can't** |
| C53 | `-TFThemeSet` calls `show(_:)` and `choose(_:for:)` | :1465 | code | `WatchTheme.swift:459-460` (`E/z-misc-greps.txt`) | **holds** |
| C54 | every `ConfettiField` constant is `fx.js`'s; 222 particles; apex 601 px; `k = height / 800 = 0.310`; sizes 0.93–2.48 × 1.86–5.27 pt, polygons 1.55–3.41 pt | :1474-1500 | reading | `E/confetti-figures.txt` constants; `fx.js:69-80,96,125-126`; 7 × 26 + 40 = 222; 19² / 0.6 = 601.7; 248 / 800 = 0.31; (3–8, 6–17, 5–11) × 0.31 | **holds** |
| C55 | the field is empty at frame 147 = 2.45 s; 1/0.0075 = 133; the same number for all eighteen | :1507-1508 | sim | baseline confetti log: `ends at frame 147` for all 16 (the Secret pair not unlocked there); 147/60 = 2.45 | **holds** (16 here) |
| C56 | `[REDACTED]` returns zero entries in `.lowFrequency` | :1512 | sim | not re-run | **can't** |
| C57 | two runs 52.6 / 51.1 fps | :1524-1525 | sim | baseline 52.0; Watch track 51.8–54.5 | **holds** |
| C58 | `Kit.shapes` union: fifteen of eighteen read as hearts-only; the generator resolves a count; tally dark [0], sunset [0,1], pink [0,1,2], superpink [1,2,3], birthday [4] | :1544-1589 | `-TFConfettiSelfTest` | baseline: dark [0], sunset [0,1], pink [0,1,2], … for 16 kits; `gen-kits.mjs:33` comment; the Secret two not in the baseline run | **holds** (16); the Secret two **can't** |
| C59 | `.accented` doc quote; `fullColor` iOS-only | :1602-1610 | SDK | `E/sdk-on-disk-2.txt` | **holds** |
| C60 | face probe: `fullColor` and `accented` identical (`#4AF07A` / `#FF6B57`; `#0F8C8C`) | :1622-1629 | `-TFFaceProbe` | not run in the baseline | **can't** |
| C61 | snapshot `v` left at 1; `read()` defaults every missing field | :1657 | code | `WatchSnapshot.swift:45`, `:89-100` | **holds** |
| C62 | `11 theme change: harbor/Manrope-ExtraLight-800 → terminal/IBMPlexMono-SemiBold changed=true` "verified end to end" | :1674-1679 | sim | baseline: `terminal/IBMPlexMono-SemiBold → paper/SourceSerif4-Regular-600 changed=true` — the snapshot moved and a reload was requested; no face was drawn (`watch.md` F6) | **holds** (snapshot + request); the redraw **can't** |
| C63 | 33 `.ttf` in the Watch app, zero in the appex; `[REDACTED](_:.process:_:)` makes them resolvable | :1686, :1697 | built product | archive 33 / 0 (`W/fonts-check.txt`); the code calls the **singular** `[REDACTED]` (`WatchSnapshot.swift:233`) since a902805 — the plural is `CT_DEPRECATED … watchos(2.0, 6.0)` (`E/sdk-on-disk.txt`) | **doesn't hold** (function name stale); the 33/0 holds |
| C64 | face probe `ttfInAppex=0 fontsRoot=TodaysFiveWatch.app facesOnDisk=33 registered=33` | :1705-1706 | `-TFFaceProbe` | not run | **can't** |
| C65 | eighteen screenshots; the Secret two via `plutil -replace` into the group container's plist; `unlocked=2 offered=18` | :1724-1742 | sim | 18 files present; the injection not re-run | **holds** (files); the injection **can't** |
| C66 | carousel out-of-focus greys `#807F7B` (sketch), `#807C75` (paper), `#817F7A` (light), `#80767B` (birthday); `#080B08` on dark | :1759-1766 | pixels | not re-sampled (no PIL here; the sample coordinates are not recorded) | **can't** (would: sample the four PNGs at the row-leaving-focus point) |
| C67 | "six of the eighteen kits are light" | :1781 | — | `E/kits-tokens-2.txt`: **seven** light-base (birthday included); ed89bd7 corrects it to seven | **doesn't hold** (seven) |
| C68 | "`Kit.shapes` is still a flattened union … not fixed at the source" | :1803-1804 | — | superseded by 3ac91b2 (the generator was fixed) — the stage-2 "did not settle" list was not updated | **doesn't hold** at HEAD (stale) |
| C69 | `run &* 2_654_435_761` refused by the archive; fixed in `UInt64`; five other literals safe | :1808-1832 | archive | HEAD `TodayView.swift:517 UInt64(bitPattern: Int64(run)) &* 0x9E37_79B1` (`E/z-last-checks.txt`); baseline 5e archive compiled the `arm64_32` slice clean (`baseline.md` §5); the sweep not repeated | **holds** (fix + archive); the sweep **can't** |
| C70 | `DoorbellTransport` is one method wide; `RealtimeTransport` has zero conformers | :1840-1850 | code | `Transport.swift:116-117` one `ring`; `SupabaseTransport.swift:114`; no `: RealtimeTransport` anywhere; tests use `DoorbellRecorder` (`E/z-misc-greps.txt`) | **holds** |
| C71 | `POLL_LIVE_MS` not moved; 20/20 never in 600 s at either interval; 138 bytes | :1875-1887 | — | `sync.js:215` 240000; `SyncTests` 137/138; the 20/20 not re-run | **holds** (constants); 20/20 **can't** |
| C72 | `realsync4.js` and `interop.mjs` both assert the unchanged poll under 60 bytes; 29 defended | :1897-1899 | reading | `interop.mjs:348` and `realsync4.js:102` `poll.bytes < 60` | **holds** (< 60 asserted; 29 is not pinned) |
| C73 | doorbell body 137 / 138 bytes pinned in `SyncTests` | :1906-1909 | swift test | `SyncTests.swift:433, 437` | **holds** |
| C74 | the REST broadcast answers HTTP 202 with and without Bearer | :1915-1916 | curl | no backend | **can't** |
| C75 | medians 54 / 32 s; re-runs 132 & 16, 144 & 12; maxima 238 vs 240, 60 vs 60 | :1929-1937 | polld | not re-run; "uniform on (0, period]" is arithmetic | **can't** (single runs) |
| C76 | `polld.js` now prints `NOT MEASURED` / `x` and exits 1 | :1952-1953 | code | `polld.js:259, 268` (`E/z-misc-greps.txt`, `E/misc-checks.txt`) | **holds** |
| C77 | local transport: 0.1 s in ten trials; a simulated 15 ms lag | :1967-1968 | polld | `sync.js:139 hook("lag") || 15` ✓; the 0.1 s not re-run | **holds** (15 ms); 0.1 s **can't** |
| C78 | `announceGone` has no Swift mirror; `MemoryTransport` does not conform; `SyncEngine` has no timer | :1984-1993 | reading | `announceGone` appears only in DECISIONS; the only conformer is the test `DoorbellRecorder` (`E/z-misc-greps.txt`); the timer not re-read | **holds** (first two); the timer **can't** cheaply |

## D. `apple/README.md`

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| D1 | 130 tests in ten suites, about nine seconds | :29 | swift test | `baseline.md` §2: 130 / 10 in 8.389 s | **holds** |
| D2 | ~1,200 random document pairs and ~900 operation sequences | :36 | fixture | `merge-cases.json.deflate` inflated: `mergeCases 1200`, `sequenceCases 900` (`E/z-misc-greps.txt`) | **holds** |
| D3 | the 18 kits and 13 font pairs; the Watch's real names + 33 `.ttf` | :37-38 | fixtures | `E/kits-tokens.txt` 18 / 2 secret / 13; `E/fonts.txt` faces 33 | **holds** |
| D4 | **62 of the 314** hex tokens appear nowhere in `theme.js`'s source | :45-46 | kit-tokens | 58 of 314 at HEAD (`E/kits-tokens-2.txt`) | **doesn't hold** (stale; 62 was 0f11ee6's count) |
| D5 | JavaScriptCore in macOS/iOS SDKs, absent from watchOS | :49-50 | SDK | `E/sdk-on-disk.txt` | **holds** |
| D6 | fontTools 4.60.2; brotli not optional; the 33 `.ttf` are committed so no Python is needed to build | :65-75 | env | `E/environment-and-archives.txt`; `E/files-and-shots.txt` (33 ttf in the tree) | **holds** |
| D7 | `gen-kits.mjs` refuses to write: 18 kits, exactly 2 secret, **252** contrast assertions against each kit's own grounds | :77-79 | gen-kits | `E/kits-tokens-2.txt` 14 floors × 18 = 252; `E/z-csstext-diff.txt`: all 252 re-computed, 0 under floor, tightest `light.dim` on ink 4.5002 | **holds** |
| D8 | the interop run creates three lists and deletes all three; twelve creates an hour per address; the ledger file | :145-148 | reading | `interop.mjs:8-10` three creates; `002_v3.sql` `private.limits()` `creates_per_hour 12` (`E/misc-checks.txt`); `.interop-created.txt` at `interop.mjs:48` | **holds** (by reading; not run) |
| D9 | the App Manager key is refused for `-exportArchive` (403) | :223-226 | — | not re-provoked | **can't** |
| D10 | five phone debug launch arguments, "each is `#if DEBUG` only" | :237-247 | code | `E/z-tf-debug-gates.txt`: all six phone read sites inside `#if DEBUG`; the archive's phone binary has 0 `-TF…` strings; the table omits `-TFDumpWatchSend` (`E/tf-arguments.txt`, `E/z-last-checks.txt`) | **holds** (DEBUG); the list is incomplete |
| D11 | `make-icon.mjs` and `tools/og.mjs` are gone | :257 | ls | `E/web-side-and-structure.txt`: both absent | **holds** |
| D12 | `--trace` renders at 180 and fails over 2 %; currently **0.30 %**; it reported 93.84 % between; 1.60 % was pre-1.11 | :266-277 | mark.mjs | `mark.mjs:210` size 180, `:214 share > 0.02`; `E/mark-trace.txt` 0.30 % | **holds** |
| D13 | the icon at 1024 in three appearances; the script counts transparent pixels and fails if any | :279-282 | mark.mjs | `mark.mjs:237-242 transparentPixels`, "(iOS requires none)" | **holds** |
| D14 | four `CustomEvent`s, no `detail`; `e2e4.js` asserts all four at both viewports | :286-296 | reading | `E/web-side-and-structure.txt`: e2e4 `tf:*` assertions at both viewports | **holds** |
| D15 | `SHELL` is read from the UA token and **exactly two** things turn on it: `HAPTIC` stands down, `STANDALONE` is true | :298-300 | grep | `app.js:28` `SHELL`, `:31` `STANDALONE`, `:188` `HAPTIC` — exactly two uses (`E/z-last-checks.txt`); `WebViewController.swift:21,107` `"TodaysFive/1"` (`E/privacy-and-structure-3.txt`) | **holds** |
| D16 | one Keychain item per link, not synchronizable | :307 | code | as A32 | **holds** |
| D17 | `tf/app/seen`; the four `VaultReconciler` rules; `archived` still honoured | :314-332 | code / swift test | `E/misc-checks.txt`: `VaultReconciler` skips `archived`; the suite ran green (`baseline.md` §2 "The link vault") | **holds** (by reading + suite) |
| D18 | "the **one** `print` in the app is `#if DEBUG`" | :336 | reading | the phone target has **three** (`WatchLinkSender.swift` ×2, `WebViewController.swift` ×1), all `#if DEBUG`, all counts (`E/structure-checks.txt`, `E/privacy-prints.txt`) | **doesn't hold** ("one"; the rule holds) |
| D19 | `watchsim.mjs`; node's path | :355-356 | — | `E/environment-and-archives.txt` | **holds** |
| D20 | `simctl install` of the phone app does not install the watch app; 7006 | :364-368 | — | as A7 | **can't** (consistent) |
| D21 | eleven Watch debug launch arguments, each `#if DEBUG` | :374-390 | code | `E/z-tf-debug-gates.txt`: all 11 Watch reads inside `#if DEBUG`; the archive's Watch binary has 0 `-TF…` strings; the Debug dylib carries all 11 | **holds** |
| D22 | `-TFFontSelfTest` walks every one of the 13 pairs | :383 | code | `WatchTheme.swift:53 for type in Kits.types.values…` (`E/font-selftest-and-scrim.txt`); baseline `pairs=13` | **holds** |
| D23 | the confetti field ends at frame 147 | :385 | sim | baseline: all 16 kits `ends at frame 147` | **holds** |
| D24 | `-TFFaceProbe`: the appex can reach the app's fonts; `.accented` does nothing in SwiftUI | :386 | sim | not run | **can't** |
| D25 | `-TFThemeSet` calls the picker's own two store methods | :387 | code | C53 | **holds** |
| D26 | the three Siri phrases | :423 | code | `TodaysFiveShortcuts.swift:36-38` | **holds** |
| D27 | Double Tap is Series 9 / Ultra 2, watchOS 11+ | :433 | SDK | A14 | **holds** |
| D28 | the same 16 kits `TodaysFiveCore` carries + the two Secret; **no** `.primary`, `.secondary` or system colour on any Watch screen | :438-442 | code | `Kit(` literals 16; HEAD Watch views: 0 `.primary`/`.secondary`; the appex keeps one `.foregroundStyle(.secondary)` (`Complications.swift:244`, `E/misc-checks.txt`), where the face's palette wins anyway | **holds** (the app's screens); the appex has one |
| D29 | keys `tf/app/watch/kit/day`, `…/night`, `…/slot` | :445 | code | `WatchTheme.swift:337-339` | **holds** |
| D30 | `DECISIONS-phase4-C1.md` / `DECISIONS-phase4-C2.md` "has the argument" | :447, :454 | ls | both files absent at HEAD (folded into `DECISIONS-apple.md` by 70d29ea); also dangling at `DECISIONS:1394` and `WatchTheme.swift:37` (`E/files-and-shots.txt`) | **doesn't hold** (dangling references) |
| D31 | a light ground drives the panel about **17×** harder than a dark one | :449 | screenshots | superseded by 27.6× (a4f180a); `DECISIONS:1372-1375` says the ratio is not a stable quantity | **doesn't hold** (stale, and the round says not to quote it) |
| D32 | no `system` mode (watchOS has no light appearance) and no `schedule` | :452-454 | reading | `DECISIONS:1414-1422`; the appearance not re-tested | **can't** (the appearance) |
| D33 | the confetti is `fx.js` port for port with every *length* scaled by `height / 800` and no *time* touched | :455-457 | code | `E/confetti-figures.txt`: `referenceHeight = 800`, `k = height/800`; the 65 ms / 210 ms unscaled | **holds** |
| D34 | the complication follows the kit's type, not its accent; `WatchSnapshot` `kit`/`pair`/`face` with `v` 1; the extension reads the app's fonts from its containing bundle | :459-464 | code | `WatchSnapshot.swift:20-25,45,233`; archive appex 0 ttf | **holds** |
| D35 | one settings screen — the theme, behind the long press | :471 | code | `-TFShow actions|theme`; `theme-picker.png`, `count-actions.png` | **holds** |
| D36 | the App Group holds a `WatchSnapshot` — a name, two counts, one line and a stamp — and **never a link** | :478-479 | reading | `W/appgroup-container.txt` (`simctl get_app_container` + 3-line Python): `lists/<id>.json` (1,929 B) beside `snapshot.json`, whose **filename equals the document's own `id`** (22 chars — the `W` of an edit link, which `SyncEngine.swift:207` strips for the wire because "a viewer must never learn W"); `watch.md` F3 | **doesn't hold** |
| D37 | every `print` is `#if DEBUG` and passes counts or a length | :480-481 | print-audit | `E/privacy-prints.txt` | **holds** |

## E. `apple/tools/watchsim.mjs` — header and comments

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| E1 | `simctl install` of the iPhone app does NOT install the embedded watch app; 7006 | :13-19, :524-527 | the round's afternoon | as A7; `doctor` names the state (:739-744) | **can't** (consistent) |
| E2 | `simctl` cannot tap a watch simulator — no `simctl ui tap`, no accessibility bridge | :21-23 | — | every instrument in this review was built around it | **can't** (consistent) |
| E3 | `safe()` strikes any run of 22+ base62 characters on the way to the terminal | :26-30, :69-73 | code | `SECRET_SHAPED = /(?<![0-9A-Za-z])[0-9A-Za-z]{22,}(?![0-9A-Za-z])/g`; the baseline's logs show it firing on a 26-char API name (`[REDACTED]=present`) | **holds** |
| E4 | `simctl launch --stdout=<path>` resolves inside the device's data root | :57-61 | measured | not re-run | **can't** |
| E5 | there is no `timeout` on this machine | :109 | which | `command -v timeout` → absent (`E/z-misc-greps.txt`) | **holds** |
| E6 | "Both bundles carry the web's frozen **1.12 (158)** all round, so the version comparison above cannot fire" | :779-782 | — | both bundles are 1.12 (216) (`baseline.md` archive plists); the build moves every round, so the version comparison at :770-774 is the one that fires first (`watch.md` F8) | **doesn't hold** (stale) |
| E7 | a pair reports `active, connected` only once both are up; can take another minute | :448-450 | — | the baseline's fresh pair read `(active, connected)` on the first try after `bootstatus` (`B/sim-summary.txt`) | **holds** (consistent) |

## F. Commit messages in range stating figures (`git log 0e03143..7341981 -- apple/`, 47 commits; the full log is in the scratchpad's `gitlog-apple.txt`)

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| F1 | a returning b158 device paints `#4AF07A` / `#C8321F`, caches stamped `1ptwfkh`; five configurations; 0 errors | 7341981 | Playwright vs deployed | `index.html:2` carries `1ptwfkh`; the runs not reproduced | **can't** |
| F2 | "Build 216 … `data-tokens-rev` stays 1ptwfkh … **Node 137**" | 948a905 | node | `git rev-list --count 948a905` = 216 ✓; `1ptwfkh` ✓; `E/node-suites-historical.txt` at 948a905: **136** `ok` lines (features 30) | **doesn't hold** (137; the suites print 136) |
| F3 | reserved names **15 across 9** (was 10/7); PT Sans nameID 0 has no OFL and nameID 14 `OFL_web` 404s; every face has a copyright and a licence URL; `OFL.txt` ships beside the 33 faces; about.html Typefaces; the `.ttf` bytes not regenerated; "Node 137" | 1aada68 | fontTools / curl | `E/font-licence-fields.txt`: PT Sans nameID 0 "ParaType … All rights reserved", nameID 14 `http://scripts.sil.org/OFL_web` → HTTP 404; no face lacks nameID 0; none lacks 13/14; `E/fonts.txt` `reservedFontName` 15 — 9 families by name (`E/z-misc-greps.txt`); archive Watch app has `OFL.txt`, no `README.md`; `about.html` Typefaces 1; ttf history is the single commit 9a2f369 (`E/files-and-shots.txt`); Node at 1aada68 = 136 | **holds** (all but "Node 137") |
| F4 | clock contrast light 1.06 · blush 1.07 · sketch 1.08 · birthday 1.08 · teletype 1.09 · **harbor 1.10** · paper 1.12; dark 16.0–19.9; **seven** light kits; six of the eight Day kits light; 76×44 scrim; after: paper 17.12:1, light 8.29:1 | ed89bd7 | pixels | `E/kits-tokens-2.txt`: 1.06 / 1.07 / 1.08 / 1.08 / 1.09 / **1.11** / 1.12 (harbor 1.105 — rounds up), 16.04–19.89; 7 light; Day 6 of 8; `WatchTheme.swift .frame(width: 76, height: 44)`; the "after" ratios not re-measured | **holds** (harbor rounds the other way); the after **can't** |
| F5 | "Build 212 … 169 + 42 + 1 … **Node 136**, swift test 130 in 10 suites" | 8a412a3 | — | rev-list 212 ✓; `@Test` 130 / 10 ✓; Node at 8a412a3: **135** (features 29) | **doesn't hold** (Node 136 → 135) |
| F6 | fifteen of eighteen byte-identical; light's danger at most 7/255 | e6f7d28 | cssText | 15 ✓ (`E/z-csstext-diff.txt`); 7, 7, 6 ✓ | **holds** |
| F7 | 32-bit `Int` on arm64_32; the archive alone refused it; the sweep found five other literals safe | 923714c | archive | HEAD uses `UInt64`; the baseline archive compiled `arm64_32` clean; the sweep not repeated | **holds** (fix + archive); the sweep **can't** |
| F8 | the live run: 0.52 s / 0.63 s / 240 s / 29 bytes / three lists | 5cd559f | ad-hoc script | the commit changes only `PLAN-apple-phase4.md` (+28); no instrument in the tree | **can't** (uncommitted) |
| F9 | Node 136, swift 130/10, e2e4 169/0, zero warnings, fonts 98/98, confetti 108/108; 54/132/144 s; 47 % vs 0 of 2,000 | fb018ce | — | Node at fb018ce = **135**; 130/10 ✓; 169/0 ✓ (baseline); 96/96 and 96/96 here (B60) | **doesn't hold** (Node 136; 98/108 as printed) |
| F10 | "Build 207 = 169 + 37 + 1"; README said 126, it is 130; Node 28/33/10/14/12/29/9 | 16aac3d | — | rev-list 207 ✓; 130 ✓; Node at 16aac3d = 135 = the list | **holds** |
| F11 | the plural `[REDACTED]` is deprecated at watchOS 6.0; the singular is deprecated nowhere; both targets zero warnings | a902805 | header | `CTFontManager.h`: plural `CT_DEPRECATED_WITH_REPLACEMENT … watchos(2.0, 6.0)`, singular `CT_AVAILABLE(… watchos(2.0) …)` (`E/sdk-on-disk.txt`); `WatchSnapshot.swift:233` singular; baseline 0 warnings | **holds** |
| F12 | "Build 202 = 169 + 32 + 1"; six homes; `#B13924` at 4.5050 "**now the worst of the eighteen**"; 14 × 18 = 252; theme 34 | 2dbdfd9 | — | 202 ✓; 4.5050 ✓; but `light.dim` on ink is 4.5002 — tighter (`E/z-csstext-diff.txt`, and 9a2f369 said so); theme 34 at 2dbdfd9 ✓ | **holds** except "worst of the eighteen" |
| F13 | the shapes union; fifteen read `[1]`; theme 34; swift 130/10 | 3ac91b2 | — | `gen-kits.mjs:33` comment; 130/10 ✓ | **holds** |
| F14 | carousel greys; "**six** of the eighteen kits are light" | e3255d0 | pixels | seven light-base (birthday); the greys not re-sampled | **doesn't hold** (six → seven); the greys **can't** |
| F15 | eighteen shots by id; two long-press screens; four finales frozen at 0.55 s | b6ce079 | ls | 18 `today-*` + `count-actions` + `theme-picker` + 4 `finale-*` (`E/files-and-shots.txt`) | **holds** (files) |
| F16 | face probe identical; 33 in the app, 0 in the appex; snapshot `v` 1; harbor → terminal on a slot flip | afd278e | sim | 33 / 0 ✓; `v = 1` ✓; the probe **can't** | **holds** (structure); the probe **can't** |
| F17 | 222 particles at 51–53 fps; frame 147; the constants | cd2c363 | sim | baseline 52.0 / frame 147 ✓ | **holds** |
| F18 | dark 0.0285 / terminal 0.0284 / paper 0.7868; 27.5–27.6×; 51.1 % of 206,336; 1.12:1 | a4f180a | screenshots | arithmetic ✓ (C49); the drives **can't** | **holds** (arithmetic) |
| F19 | fourteen call sites across five files; Lato Black at 16pt 19.20 tall vs 18.24 | 4ca4cb0 | — | 14 / 5 ✓; hhea at 16pt = 19.2 (`E/fonts.txt`); 1.14 × 16 = 18.24 | **holds** |
| F20 | "four build homes plus the sixth"; the nth-child(5) → (6) trap | e809d78 | — | as B51/B52 | **holds** (the miscount noted) |
| F21 | refs 264–296, build files 436–468, group 312; 33 ttf in the app, README absent; Node 136 (theme 34, features 29); swift 130/10; `--trace` corrected to 0.30 | 295134a | — | `E/pbxproj-and-plists.txt` bands (34 each now, `OFL.txt` added later), group 312 present; archive 33 ttf, no README, `OFL.txt`; Node at 295134a = 136 ✓; 0.30 ✓ | **holds** |
| F22 | 126 in ten; Node 28/32/10/14/12/28/9 | 144e5b3 | — | `E/test-counts-corrected.txt` 126 / 10; node 133 ✓ | **holds** |
| F23 | both kits plus a link is a 1,178-byte context; object files `A86014` ×2, `FF2E9A` ×0, `D62E86` ×0 on both platforms; 126 tests in 10 suites | 7f0e112 | strings | 126 / 10 ✓; `E/built-products.txt`: the macOS `Kits.generated.swift.o` has `A86014` ×1, `FF2E9A` ×0 (the per-platform `.o` files are not on disk); 1,178 not re-measured | **holds** (126; the absences); 1,178 and the ×2 **can't** |
| F24 | 21,380 bytes drift; 19.6 KB against ARG_MAX 1 MB; 16 of 26 tokens cross; 122 tests in 10 suites, 8.7 s; tightest light accent/ink3 3.0016 | 0f11ee6 | — | `E/structure-checks.txt` 21380 at 0f11ee6; 19,614 B and ARG_MAX 1048576 (`E/kits-tokens.txt`); `hexTokens` 16 (`E/kits-tokens-2.txt`); 122 / 10 ✓; 3.0016 ✓ | **holds** (8.7 s **can't**) |
| F25 | 137 / 138 bytes; HTTP 202 with and without the bearer; swift 120 in 9 | c21c31c | — | 137/138 pinned ✓; 120 / 9 ✓; 202 **can't** | **holds** (2 of 3) |
| F26 | 234 contrast assertions; tightest light `dim` 4.5002; 33 TTF **1.18 MB from 688 KB**; **12** of 22 not 10; source-serif-4 has no opsz; Fraunces opsz pinned at 9; 33 files not 26; **ten** faces carry an RFN | 9a2f369 | — | 13 × 18 = 234 ✓; 4.5002 ✓; 1.18 MiB / 688 KiB (704,776 B, 24 files) ✓; source-serif-4 axes `wght` only, fraunces `opsz` default 9 ✓ (`E/fonts.txt`); "ten faces" corrected to 15 by 1aada68 | **holds** except the RFN count (known-wrong, corrected since) |
| F27 | 1.54 MB vs 6.23 MB; 10 of 22; three kits carry the brand accent; 3.48 → 11.22 | 176e022 | — | 1.54 MiB ✓; three ✓; 11.2212 ✓; 6.23 and 10-of-22 **can't** | **holds** (3 of 4) |
| F28 | live run rev 6 → 13; 29 vs 621; swift 116/9; Node 132; the web moved zero bytes; the archive "TodaysFive 1.12 (158) Watch.xcarchive" | 4962f97 | — | 116/9 ✓; 132 ✓; `git diff --stat e215e65 4962f97 -- . ':!apple'` empty ✓; the archive exists with 1.12 (158) ×3 (`E/environment-and-archives.txt`); the live run **can't** | **holds** (suites, diff, archive) |
| F29 | the hand-off log lines | d44516b | sim | the Watch track reproduced the channel with `links=0` (A58) | **holds** (channel) |
| F30 | the self-test lines (0.333 s, 0.707 s …); 116 tests; zero warnings; every print DEBUG | 0354b5f | — | A53–A57, A47 | **holds** (as rhythm) |
| F31 | 116 in 9 up from 94; the codec flaked 3 in 8, passes 6 for 6 | 8e3d5af | — | `E/test-counts-corrected.txt` 8e3d5af 116 / 9 ✓; the flake **can't** | **holds** (count) |
| F32 | swift test 94/94; the core builds for watchOS; the Watch app embedded and signed with the group | 7d9515f | — | 94 / 7 at 7d9515f ✓; entitlements ✓ | **holds** |
| F33 | 1.020 s; `import CoreHaptics` the entire project change; `-TFSelfTest` prints `ok duration=1.020s hardware=no` | ee5a1df | — | 0.700 + 0.320 = 1.020 ✓; pbxproj framework lines 0 ✓; the print not run | **holds** (arithmetic/structure); the print **can't** |
| F34 | two callbacks vs eleven; 256 KiB / 64 KiB; `transferUserInfo` never delivered; `isReachable` asymmetric | 9b0eba4, f7cf2ff, 7d9515f | probes | 262144 / 65536 ✓ (W); the rest **can't** | **holds** (limits) |

## G. Code comments stating figures (`E/z-code-comment-figures.txt`, 40 comment lines under `apple/` with a unit-bearing number, plus `E/code-comment-figures-1.txt`; and `Fonts/README.md`)

| # | claimed | where | measured then | measured now | verdict |
|---|---|---|---|---|---|
| G1 | seven bursts 65 ms apart, a fuller one at 210, the chord at `t0 + 0.7` | `Haptics.swift:11-12,29-30`; `WatchHaptics.swift:12-16,43-44,80-82` | fx.js / packs.js | `fx.js:125-126`, `packs.js:280` | **holds** |
| G2 | "On Paper's cream (`#F7F2E8`) white-on-cream is about **1.06:1**" | `WatchTheme.swift:31-32` | — | white vs `#F7F2E8` = 1.12; 1.06 is Light's; the same file at :648-649 says Paper 1.12 | **doesn't hold** (wrong kit's number) |
| G3 | Paper's cream drives the panel about **17×** harder; "see `DECISIONS-phase4-C1.md`" | `WatchTheme.swift:36-37` | screenshots | 27.6× per a4f180a, and the file it cites does not exist | **doesn't hold** (stale + dangling) |
| G4 | the scrim selects exactly the seven light-base kits (1.06–1.12) and none of the eleven dark (16.0–19.9); a factor of fourteen between the groups | `WatchTheme.swift:65-66` | — | `E/kits-tokens-2.txt`: 7 light 1.06–1.12, 11 dark 16.04–19.89; 16.04 / 1.12 = 14.3 | **holds** |
| G5 | `Color(white: 0.62)` was reaching for the kit's `dim`, `#8F8C84` | `WatchTheme.swift:133-136` | — | dark `dim = #8F8C84` (`E/z-contrast-arithmetic.txt`) | **holds** |
| G6 | Fraunces and Source Serif 4 carry no `fvar` named instances | `WatchTheme.swift:166`, `Kits.swift:122` | fontTools | `E/fonts.txt` | **holds** |
| G7 | a face's own line height is about 1.2–1.4× the point size | `WatchTheme.swift:189` | — | Lato Black 19.2 / 16 = 1.2 | **holds** (one face checked) |
| G8 | the clock list incl. harbor 1.10; Paper 1.12 → 17.12:1; the cream is 3× too bright; the first darkening step to 3:1 breaks all five Paper floors (text 15.34 → 5.45) | `WatchTheme.swift:645-663` | screenshots / arithmetic | harbor 1.105 (rounding); 17.12 and the darkening search not re-run | **holds** (the list); the rest **can't** |
| G9 | the time occupies pt (151,20)–(191,31); 76 × 44 covers it; a first attempt at 62 × 26 did not | `WatchTheme.swift:675-678` | screenshot | `.frame(width: 76, height: 44)` ✓; the glyph box **can't** | **holds** (the frame) |
| G10 | 16 open kits name only 11 of 13 pairs; 2 of the 33 files would go untested | `WatchTheme.swift:759-761` | — | `E/kits-tokens-2.txt` | **holds** |
| G11 | IBM Plex Mono Regular and SemiBold set the same 21-char ruler | `WatchTheme.swift:822` | CoreText | 214.2 both (`E/fonts.txt`) | **holds** |
| G12 | the 17x emissive-drive ratio | `TodayView.swift:229` | — | as G3 | **doesn't hold** (stale) |
| G13 | `fx.js` constants; 222 particles; apex 601 px; `k = 0.31` | `TodayView.swift:237-254` | — | C54 | **holds** |
| G14 | "the field is provably empty by **140** (`1 / 0.0075`)" | `TodayView.swift:387-388` | arithmetic | 1/0.0075 = 133 frames after the last burst, which is at 210 ms ≈ frame 13 → 146; the self-test measures **147** on every kit (`DECISIONS:1507`, baseline) | **doesn't hold** (140 < 147 measured; the arithmetic forgets the 210 ms) |
| G15 | "180 rotating particles drawn live on a 26.5 simulator" | `TodayView.swift:451` | — | the field is 222 | **doesn't hold** (stale checkpoint number) |
| G16 | arm64_32 has a 32-bit `Int`; 2,654,435,761 > `Int32.max` | `TodayView.swift:522-526` | archive | 2,654,435,761 > 2,147,483,647; the baseline archive built the slice | **holds** |
| G17 | "The Series 11 46mm, in points" 208 × 248 | `TodayView.swift:686` | — | a constant; the baseline's 42 mm run also printed `canvas=208x248` — the size is the constant, not the device | **holds** (as a constant) |
| G18 | poll moved 60 s → 240 s; "a write took a **median of 132 s** to appear and as long as 238 s"; zero requests at idle | `SyncEngine.swift:256-264` | polld | constants ✓; the 132 s is one run's median, which the same round's DECISIONS:1929-1942 says not to quote | **holds** (constants); the median is a single-run number |
| G19 | HTTP 202; 138 bytes; the 29-byte unchanged poll | `SupabaseTransport.swift:104-107,145` | — | 138 pinned ✓; 202 and 29 **can't** | **holds** (138) |
| G20 | 62 of the 314; 14-iteration; 60-step; 26 tokens of which 16 are `#RRGGBB` | `Kits.swift:9-12,25` | — | 58 now; loops ✓; 16 ✓ | **doesn't hold** (62 stale); the rest holds |
| G21 | "twelve of the twenty-two resolve to something else on Apple platforms" | `Kits.swift:99` | — | inconsistent with the ten elsewhere (B14) | **can't** |
| G22 | 62 of the 314; "Measured: the source is **~28 KB** against an ARG_MAX of 1 MB" | `KitsGen.swift:5,32` | — | 58; the generated file is 19,614 B (`E/z-misc-greps.txt`) — the plugin's own commit 0f11ee6 said 19.6 KB | **doesn't hold** (both stale) |
| G23 | the app's 33 `.ttf` are two levels up from the appex | `Complications.swift:79` | built product | archive 33 / 0 | **holds** |
| G24 | `TextFieldLink` has four initializers | `WatchDictation.swift:7` | SDK | 3 (`E/structure-checks-2.txt`) | **doesn't hold** |
| G25 | 10 of 22 → Helvetica; "reported 10 of 33 where the true count is 15" | `gen-watch-fonts.py:18,228,249` | — | 15 in the fixture ✓; 10-of-22 **can't** | **holds** (15); the 10 of 22 **can't** |
| G26 | "Both bundles carry the web's frozen 1.12 (158)" | `watchsim.mjs:779` | — | E6 | **doesn't hold** |
| G27 | twelve of the twenty-two; 33 faces, 1.18 MB, from 24 of 26 woff2 (688 KB); fifteen of 33 across nine families; "was 10 of 7"; PT Sans nameID 0 / 14 404; per-file sizes (lato-900 28 KB, archivo 80 KB …) | `Fonts/README.md:15,44,111-113,126,130` | fontTools / curl | 33 = 1,238,128 B = 1.18 MiB; 24 files = 704,776 B = 688 KiB (fixture `totals`); 15 faces / 9 families ✓; PT Sans ✓ (`E/font-licence-fields.txt`); lato-900 28,520 B, archivo 40,980 + 40,900 = 81,880 B ✓ (`E/z-misc-greps.txt`) | **holds** (the twelve-vs-ten unresolved) |
| G28 | "Measured empty in every run, which is why the callback below is the delivery path" | `WatchLinkReceiver.swift:218-219` | — | contradicted by the same file's header and DECISIONS:586-591 (`watch.md` F8) | **doesn't hold** |
| G29 | "Track C's `AddFlowView` calls this, and so does the App Intent" | `WatchStore.swift:509-510` | — | `watch.md` F8/F2: `WatchStore.add` has no caller | **doesn't hold** |
| G30 | two prebuild plugins verified against both an iOS and a watchOS build | `Package.swift` comment | xcodebuild | baseline 5a–5e | **holds** |
| G31 | `p_rev`, not `p_known_rev` | `SupabaseTransport.swift:2-4` | — | `002_v3.sql:165` | **holds** |

---

## Traps found

1. **One-answer instruments — a check that cannot fail.** `-TFWatchSelfTest` prints eleven observations and judges none of them (`watch.md` F6: `fired=NO` and `taps=7/8` would print as calmly as their passes; there is no `pass=N/M`, no exit code). `PLAN-apple-phase3.md:626-640` and `README.md:381` then read the printout as proof. The phone's `-TFSelfTest` "finale pattern ok duration=1.020s" (`DECISIONS:469,505`) reports `CHHapticPattern.duration`, which is the sum of the constants the same file wrote — it can only say 1.020 (C2). The pre-c0b8e2b `polld.js` printed "never, 0/0" when the server was dead (`DECISIONS:1944-1953`); fixed in the round. `-TFAddSelfTest`, `-TFFontSelfTest`, `-TFConfettiSelfTest` do count (`end pass=N/M`).
2. **Branch-proof cited as feature.** Every one of the 17 `-TF…` arguments is read inside `#if DEBUG` (19 read sites, `E/z-tf-debug-gates.txt`), and the Release archive's phone and Watch binaries contain **none** of the 17 strings — so a TestFlight build has no self-test at all, and every "green" above is a Debug-simulator sentence. The named example is `[REDACTED]=present` (`PLAN3:639,696`, `DECISIONS:883`, `README:382`): it proves which controller will be asked, not that a wrist shows a microphone. `README.md`'s table also omits `-TFDumpWatchSend`.
3. **Single-run medians.** `PLAN4:87-91` (54 s / 32 s from ten trials; the checkpoint table still stands uncorrected in place), `SyncEngine.swift:258` ("a median of 132 s" — a second single run, now a code comment), `PLAN3:630` `after=0.333s` (0.51 s under load 150, 0.33–0.36 s otherwise), `DECISIONS:1524` two fps runs, and the 0.63 s live figure (one trial, uncommitted script).
4. **Wrong-list counts.** `PLAN4:421` "136 tests — … features 29 …" sums to 135; the true 136 has features 30 (`baseline.md` §1). Commit messages: fb018ce and 8a412a3 say "Node 136" at commits whose suites print 135; 948a905 and 1aada68 say "Node 137" where they print 136 (`E/node-suites-historical.txt`). `E/test-counts.txt` was itself a wrong-list instrument (a loop that counted 0 `@Test` everywhere) and was corrected in `E/test-counts-corrected.txt` — the predecessors left both, which is right.
5. **Fragile denominators.** a4f180a: the dark frame's drive went 0.0466 → 0.0285 between two runs of the same layout, moving the ratio 17.5× → 27.6×; the round wrote down that a ratio with a near-zero denominator "does not support three significant figures" and then `README.md:449`, `WatchTheme.swift:36` and `TodayView.swift:229` went on quoting 17×. Same shape: `derive()`'s "~47 %" was uniform-random hex (B71), and the two counts of the same 3,000-seed run disagree between PLAN and DECISIONS (1,152 vs 1,139; 1,687 vs 1,690).
6. **Uncommitted scripts.** 5cd559f's 0.63 s: the commit adds 28 lines to `PLAN-apple-phase4.md` and nothing else; no file in the tree prints `live=true` or the number. The committed instrument for the same question is `interop.mjs` step 5b (`DOORBELL_MS = POLL_MS / 4`), which "was written and not run" (f1d09e1). The 27.6× drive script is likewise not in the tree.
7. **One-destination "zero warnings".** `PLAN3:620` counts "either configuration" of the watchOS Simulator build; `PLAN4:424` counts the iOS Simulator and watchOS Simulator builds. The baseline widened this to five `xcodebuild` destinations plus the archive, all 0 `warning:` lines (`baseline.md` §5) — so the claim survives a wider instrument — but `swift test` at HEAD prints 2 warnings in the test target (`SyncTests.swift:122-123`), which neither write-up mentions.
8. **Screenshots-as-test.** e3255d0: the carousel's ~50 % grey band exists only as four hex values read off PNGs; nothing asserts it, no coordinates are recorded, and it cannot be re-sampled here (C66). The 17.5× / 27.6× drive ratio and ed89bd7's clock contrasts are also screenshot-derived — the latter re-derived here from the kit inks and it holds (harbor 1.105 rounds to 1.11, the commit wrote 1.10).
9. **Stale numbers still standing at HEAD** (each is a *doesn't hold* row above): `watchsim.mjs:779` "1.12 (158)" (216, and the comparison it says cannot fire is the one that fires); 17× at `README:449` / `WatchTheme.swift:36` / `TodayView.swift:229`; `-TFFontSelfTest` 98/98 and `-TFConfettiSelfTest` 108/108 (`PLAN4:425`, fb018ce) against 96/96 on a fresh simulator — the denominator moves with the Secret unlock, and the write-up does not say the simulator was unlocked; `DECISIONS:1255,1282` "17pt title3 scales to 16.00 / 19.20 / −0.400" against the baseline's "17.00 / 20.40 / −0.425" (a text-size setting, not a font fact); `DECISIONS:1697` the plural `[REDACTED]` (a902805 moved to the singular); "62 of the 314" at `README:45`, `Kits.swift:9`, `KitsGen.swift:5` (58 now); `KitsGen.swift:32` "~28 KB" (19,614 B); "180 particles" at `PLAN4:266` and `TodayView.swift:451` (222); `PLAN4:173-175` "#1A1D21 … Fixed." (not fixed); `PLAN4:179` "1.60 % — verified" (it was 93.84 %); `PLAN4:255` "twelve" call sites (14); "exactly two kits moved" at `PLAN4:430,549` and `DECISIONS:931` (three); `README:336` "the one print" (three); `README:447,454`, `DECISIONS:1394`, `WatchTheme.swift:37` cite `DECISIONS-phase4-C1.md`/`C2.md`, which do not exist; `DECISIONS:1781` and e3255d0 "six of the eighteen kits are light" (seven); `DECISIONS:1803-1804` "shapes … not fixed at the source" (fixed by 3ac91b2); 2dbdfd9 "#B13924 … now the worst of the eighteen" (light's `dim` at 4.5002 is tighter); "1.3 MB" (1,238,128 B = 1.24 MB / 1.18 MiB; 1.3 is the directory in KiB with `OFL.txt` and `README.md`); `WatchTheme.swift:31` "Paper … about 1.06:1" (1.12; 1.06 is Light); `TodayView.swift:388` "empty by 140" (147 measured); "four initializers" ×3 (3); `PLAN3:190` the one-thing footer strings (absent); `PLAN3:407` `.enabled` (code: `.disabled`); the 10-vs-12 Helvetica count.
10. **The privacy sentence.** `README:478-479` "never a link" and `PLAN3:463-464`: the App Group's `lists/` directory names each record file by the document's `id`, which for an edit link is `W` (`W/appgroup-container.txt`: `filename==doc.id: True`, 22 chars). The redaction rule the repo uses (`safe()`, 22+ base62) and the App Group disagree about what is secret-shaped. Everything else in the privacy set holds: `[REDACTED] false` in one file compiled into both targets; `-TFDumpWatchSend` prints counts and sorted key names only; all 30 app prints are `#if DEBUG` with counts or lengths; `[REDACTED]` is 262144 in the watchOS runtime.

## Known-wrong items — what became of each

- **Reserved-name count 10 faces / 7 families → 15 / 9 (1aada68).** Re-derived from `test/fixtures/watch-fonts.json`: 15 faces with `reservedFontName`, all in the nine families `Fonts/README.md:126-128` names (`E/z-misc-greps.txt`, `E/fonts.txt`). Every place either number appears at 7341981: `Fonts/README.md:126` (15/nine — current), `:130` ("was 10 of 7" — as history), `gen-watch-fonts.py:228,249` (the old 10 of 33 — as history), `gen-watch-fonts.py:368-370` (prints the count from the fixture), 9a2f369's message ("Ten faces" — immutable). No live sentence still asserts 10/7. Whether the 15 must be renamed is left open in `Fonts/README.md:123-143`; the `.ttf` bytes were not regenerated (one commit in their history, `E/files-and-shots.txt`).
- **The 54 s median (fb018ce).** Corrected in `PLAN4:487-491` and `DECISIONS:1927-1942` ("the median was not the finding"); the checkpoint table at `PLAN4:87-91` still prints `54 s` / `32 s` as medians with no note, and `SyncEngine.swift:258` now carries a different single run ("a median of 132 s") as a code comment. `tools/polld.js` is committed (f4c5faa, c0b8e2b) and was not re-run here (Playwright under load); what reproduces by arithmetic is the 4× ratio `POLL_LIVE_MS / POLL_MS` = 240000 / 60000.
- **The coincidental crash fix (`DECISIONS:821-866`, `PLAN3:605-611`).** The `[REDACTED]` key that "fixed" 24 appex crashes was shown by a control run not to have; HEAD's `ComplicationsInfo.plist` carries `NSExtension` → `com.apple.widgetkit-extension` only and `[REDACTED]` appears nowhere under `apple/` (`E/z-last-checks.txt`). Nothing to re-measure; the record is the control run.
- **The simulator lying twice.** (a) The dictation branch: `-TFAddSelfTest` prints `[REDACTED]=present` on every simulator run (baseline, `[REDACTED]=present`); it settles which controller will be presented, not what a wrist shows — still open, Phase 5's. (b) The 32-bit `Int` on arm64_32 (923714c): every simulator build and `swift test` accepted `run &* 2_654_435_761`; only `xcodebuild archive` for iOS (which builds the `arm64_32` slice) refused it. HEAD does the multiply in `UInt64` (`TodayView.swift:517`), and the baseline's archive compiled the `arm64_32` slice clean — `baseline.md` §5 calls it "the one instrument in the battery that compiled the Watch code with a 32-bit `Int`".
- **"0 of 2,000 themes" (Ink track's).** Appears at `PLAN-apple-phase4.md:506` ("Surprise me over 2,000 themes moved 0") and in fb018ce's message; nowhere else under `apple/`. Not assessed here.

## Can't verify here — what would (pass/fail form)

Wrist-only, the write-ups' own list plus what the inventory added:
- **Dictation.** Tap `+` on a Series 9+/Ultra 2 (watchOS 11+). PASS: the first screen is the microphone. FAIL: a keyboard, or the input chooser.
- **Double Tap.** With the app open, double tap. PASS: the add flow starts. FAIL: nothing, or the list scrolls.
- **Action button (Ultra).** Settings → Action Button → Shortcut lists "Add to Today's Five"; pressing it starts the add flow. PASS/FAIL on both halves.
- **Siri.** Say "Add to Today's Five". PASS: Siri asks for the line (spoken), the line lands on Today within one poll of a phone. FAIL: unrecognised, or the dialog only shown.
- **The finale in the hand.** Cross the last line off. PASS: seven distinct clicks then a stronger tap ≈ 0.7 s after the first. FAIL: fewer than seven felt (coalesced).
- **Complications.** Add `accessoryRectangular` with the line switch on, and `accessoryCircular`, to a face with a custom accent. PASS (type): the count is set in the kit's face, not the system font. PASS (accent, as the round predicts): the face's palette wins and the kit's accent does not show. FAIL (type): system font — the appex's font path does not work under the widget host.
- **Paper on a wrist.** Choose Paper for the Day slot. PASS: the clock reads on its scrim and the carousel's out-of-focus grey band reads as intended. FAIL: the band reads as broken, or the scrim is visible as a patch.
- **60 fps.** Finale on real silicon. PASS: no visible stutter through the 2.45 s field. FAIL: dropped frames.
- **The doorbell across suspend/resume.** Background the phone's app, cross a line off on the Watch, foreground the phone. PASS: the line is there within ~1 s of foregrounding. FAIL: it arrives on the next 240 s poll.
- **Battery.** Nothing on this machine produces watts; the 27.6× figure is emissive drive at γ 2.2 with three caveats. PASS/FAIL only from a day of wear on Paper vs Dark with the same list.
- **App Store review.** Bundled OFL fonts carrying reserved names (15 faces). PASS: accepted. FAIL: renamed via one line in `set_names` and a regenerated map.

Not wrist, but not runnable here (machine rule: no builds, no simulators, no backend):
- the 10-vs-12 Helvetica count — register the 26 converted TTFs in one CoreText process and ask `CTFontCreateWithName` for the 22 CSS names; expect ≥ 10 to answer a Helvetica descriptor;
- the 394 / 1,178-byte payload sizes — `swift test` and read `WatchLinkTests`' print;
- 29 bytes vs 593/621 — `tfive raw` against one live list;
- 0.52 / 0.63 s — `interop.mjs` step 5b against the deployed page, ten trials, the distribution not the median;
- `.preferredColorScheme(.light)` inert, `\.colorScheme == .dark`, `[REDACTED]` empty in `.lowFrequency`, `ImageRenderer` `.accented` byte-identical, the picker across a cold launch — each is one launch on the review pair with the existing `-TF…` argument;
- the 27.6× drive and the carousel greys — the two `-TFKit` shots and a committed pixel script (the round's is not in the tree);
- the latency table — `tools/polld.js` at load < 5, two seeds, reporting the distribution;
- the Action button API absence — `grep -ril actionbutton` over the SDK.
