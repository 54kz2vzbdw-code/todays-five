# Claims-web — every number and checkable assertion in the web-side write-ups of `0e03143..7341981`

## Header

- **Worktree**: `/Users/pricebrannen/Today's Five/todays-five-review-claims-web`, detached at `7341981` (`git rev-parse HEAD` = `734198124e6f…7d043ed7`, the full sha printed at open), read-only. `pwd` at open: `/Users/pricebrannen/Today's Five/Swift Code`.
- **Range**: 67 commits; 25 touch web files (`git log --format='%h %s%n%b' 0e03143..7341981 -- . ':!apple'`); the rest are `apple/`-only but several of their messages state web-side figures and are inventoried below.
- **Write-ups covered**: `PLAN.md` 1574–1779 (the 1.12 section; PLAN.md carries no Phase 3/4 section — the web halves of Phase 3/4 live in `apple/PLAN-apple-phase4.md` §Results/§What a person sees/§What a returning device sees and in `apple/DECISIONS-apple.md` Track A/B/D, and are inventoried where they state a web-side figure), `COMPATIBILITY.md` (§5 and §8 changed in range), `README.md` (unchanged in range), `whatsnew.json`, `version.js`, `about.html`, `sw.js`, `index.html`, the 25 web-side commit messages plus the apple-side ones that state web figures (`7341981`, `e6f7d28`, `fb018ce`, `5cd559f`, `e3255d0`, `a902805`, `e809d78`, `295134a`, `9a2f369`), and code comments stating figures in `app.js`, `panels.js`, `model.js`, `sync.js`, `theme.js`, `fx.js`, `sound.js`, `sw.js`, `tools/*.js`, `test/*.js` (the comments added in range: `git diff 0e03143..7341981 -- <those files> | grep '^+' | grep -E '//|/\*' | grep -E '[0-9]'`).
- **Evidence read (all 21 predecessor files)**: `suite-counts.txt` (seven Node suites on 20 commits via `tools/suite-counts.sh`), `gzip-sizes.txt` (zlib level 6/9 + raw for 19 files at 0e03143 / 74bf09f / a53bde8 / 7341981), `firstpaint.txt` + `firstpaint.pid` (8 interleaved rounds of `tools/firstpaint.mjs`; pid 12303 no longer running), `contrast.txt` (`tools/contrast.mjs` over the worktree's own `theme.js`), `csp-hashes.txt`, `appjs-comment-count.txt` + `appjs-151-157.diff` (the `app.js` diff 0e03143..74bf09f), `kitshots.log` + `kitshots-timing.txt` (one `tools/kitshots.js` run: 18 shots, 9.26 s wall, exit 0), `serve-8894.log` + `serve-8894.pid` (the predecessor's server, pid 12265, dead; it served a scratch `trees/` root, not the worktree), `tools/{suite-counts.sh,contrast.mjs,firstpaint.mjs}`, and `live/`.
- **`live/` — what it holds, exactly**: six files — `about.html` (10,906 B), `index.html` (51,206 B), `sw.js` (5,413 B), `theme.js` (60,259 B), `version.js` (474 B) and `fetched-at.txt` reading `Thu Sep 10 06:06:48 UTC 2026`. All five page files are **byte-identical to the worktree at 7341981** (`cmp`). No source URL, command or transcript was recorded. The directory name, the `fetched-at` stamp and its timing (01:06 local, fifteen minutes before the predecessor's own 8894 server existed) say these were **fetched from the deployed site** (`https://54kz2vzbdw-code.github.io/todays-five/`, the page's own canonical link) — i.e. a **live URL fetch of five static files happened**, which the brief reserved for the orchestrator. Stated plainly so the orchestrator can count it. What it was **not**: no list was created (the files are static assets containing no list id or secret, and none of the predecessor's logs shows a `tfive new`, a `realsync4.js` run, a `polld.js` run against anything but loopback, or a Supabase request). Nothing in this report depends on `live/`; every check here was made against the worktree.
- **Instruments run by this track** (all local, `?transport=local`, port 8894 serving the worktree, one Chrome-driving script at a time, server pid recorded in the task log and killed at the end): `tools/z-arith.mjs` → `z-arith.txt`; `tools/z-shapes.mjs` → `z-shapes-and-hex.txt`; `tools/z-stamp.mjs` → `z-stamp.txt`; `tools/z-suite-counts.sh 295134a` → `z-suite-counts-295134a.txt`; the features suite on a copy with a mismatched `data-tokens-rev` → `z-features-tokens-mismatch.log`; `tools/polld.js --only fg-dead|fg-live --trials 10 --seed 1|2|3` → `z-polld-*.log`, `z-polld-summary.txt`. Everything else reuses the predecessor's evidence, `review/raw/baseline.md` (Node 136 = model 28, theme 33, crypto 10, sync 14, sound 12, features 30, compat 9; e2e4 169/0 at both viewports) and `review/raw/cleanup.md` where cited.
- **Not run, by rule**: `tools/e2e4.js` in full, `tools/realsync4.js`, `polld.js` against anything but loopback, `tools/mark.mjs --trace` (the Ink track's), `swift test`, `xcodebuild`, simulators, Lighthouse (not installed: `command -v npm npx pnpm lighthouse` → all absent; the codex runtime carries only `node`, `playwright`, `playwright-core`).

**Totals** (the leading verdict of each row, 154 rows: A 33 · B 13 · C 7 · D 6 · E 14 · F 53 · G 16 · H 12): **holds 96 · doesn't hold 32 · can't 26**.

## The inventory

### A. PLAN.md — the 1.12 section (1574–1779)

| # | claimed | where | how measured then | how measured now (command · evidence) | verdict |
|---|---|---|---|---|---|
| A1 | Node suites "model 28, theme 31, crypto 10, sync 14, sound 12, features 28, compat 9" (=132) | PLAN.md:1702 | the suites | `suite-counts.sh 74bf09f a53bde8` → exactly those seven tallies, 132 `ok -` (`suite-counts.txt`); cleanup ran 67f35a5 to the same seven | holds |
| A2 | `swift test` — 94 tests in 7 suites | PLAN.md:1703 | `swift test` | not run here (rule); cleanup counted 94 `@Test` / 7 `@Suite` at 67f35a5 by grep (`cleanup.md` claims table) | can't (consistent by count) |
| A3 | Browser suite 169 tests, ten of them this round's, five a side | PLAN.md:1704 | the suite's tally | baseline run 2: `169 passed, 0 failed` (85 desktop + 84 phone, `baseline.md` §4); cleanup: 5 new `1.12` tests per side at 67f35a5 and head | holds |
| A4 | "The last full run was 168 passing and one failing … fixed and re-run green on its own" | PLAN.md:1704; a53bde8 | one run then | that run cannot be re-seen; the fix (a53bde8, one more `t.esc()`) is in and the head suite is 169/0 (baseline) | holds at head |
| A5 | "Zero page errors, zero CSP violations, zero third-party requests in every run" | PLAN.md:1704 | the suite's safety test | baseline e2e4 log: both `ok - … no page errors, CSP violations or third-party requests across a full session` lines (cleanup HOLDS-l cites lines 87/173) | holds at head ("every run" then: can't) |
| A6 | Real backend "6 passed, 6 of 6 lists cleaned up. Unchanged poll 29 bytes; a realistic list 6,509 bytes encrypted" | PLAN.md:1705 | `tools/realsync4.js` | not run (no real backend, no list creation) | can't |
| A7 | `-TFSelfTest` "heard=4/4" and "finale pattern ok duration=1.020s" | PLAN.md:1706 | the simulator | not run; cleanup: `0.700 + 0.320 = 1.020` holds against `Haptics.swift:35–37`, no phone self-test log in baseline | can't |
| A8 | "Lighthouse could not be run … not installed in this machine's Node runtime, and there is no npm, npx or pnpm here" | PLAN.md:1710–1711 | the machine | `command -v npm npx pnpm lighthouse` → all absent; runtime `node_modules` holds only playwright | holds |
| A9 | First paint "measured directly through CDP with Lighthouse's own mobile throttling numbers … 150 ms RTT, 1.6 Mbps down, 4× CPU. Eight runs a side, interleaved and order-flipped" | PLAN.md:1714–1717 | an unnamed script | `git log --all --format='%h %ad %s' -- tools/` (86 commits, no paint/perf script ever added); `git log --all -p -S'emulateNetworkCond'` (the CDP throttling call's name) → only `32e5328` `tools/audit/harness.mjs` (`slow3g`: 400 ms / 50 KB/s, no CPU throttle). The instrument was never committed (cleanup PAPERCUT-9 found the same). The predecessor's stand-in `tools/firstpaint.mjs` re-implements the description | can't (uncommitted instrument; stand-in below) |
| A10 | desktop FCP/LCP 56 ms (44–64) before, 56 ms (44–60) after — "Desktop is unchanged" | PLAN.md:1721–1722, 1726 | that script | `firstpaint.mjs`, 8 rounds, 0e03143 vs 74bf09f: desktop 58 (44–116) vs 56 (48–64) (`firstpaint.txt`) | holds |
| A11 | on the wire 191.4 KB before / 193.8 KB after | PLAN.md:1721–1724 | that script (basis unstated) | `firstpaint.txt`: 228.2 KB vs 230.6 KB summed over CDP `encodedDataLength`, 16 requests. The **delta (+2.4 KB) matches to the decimal**; the absolute basis of the original is unstated (a different request set or header accounting) | can't (absolute) · holds (delta) |
| A12 | mobile FCP/LCP 1056 ms (1052–1108) before, **1108** (1088–1116) after | PLAN.md:1723–1724 | that script | `firstpaint.txt`: 1144 (1116–1180) vs 1170 (1152–1204) — direction the same, absolutes 90–60 ms higher (a different session and load) | doesn't hold (absolutes) · holds (direction) |
| A13 | "Mobile is about 50 ms slower, and that is a real number, not noise — the medians are 52 ms apart and the ranges barely touch" | PLAN.md:1726–1727; 74bf09f | that script | `firstpaint.txt`: medians **26 ms** apart; ranges 1116–1180 and 1152–1204 **overlap by 28 ms** | doesn't hold as stated (half the gap, inside the overlap) |
| A14 | "by about 50 ms on a 1056 ms baseline — a little under 4 %" | PLAN.md:1731–1732 | arithmetic on A12 | 26 / 1144 = 2.3 % on the re-run | doesn't hold (fragile numerator) |
| A15 | "The critical path carries **+1.86 KB gzipped**: `app.js` +1,423, `model.js` +356, `styles.css` +91, `index.html` −13. (`panels.js` … +1,466 …)" | PLAN.md:1734–1735; 74bf09f | unstated gzip | cleanup `review/raw/cleanup/i-gzip-sizes.txt` (`gzip -9` of `git show <sha>:<file>`): all five match **67cdf4b**; at shipped 67f35a5 `app.js` **+1,610**, total **+2,043 B**. Predecessor `gzip-sizes.txt` (zlib) 0e03143→74bf09f: `app.js` +1,623 (l6) / +1,629 (l9), critical-path sum 2,058 / 2,067 | doesn't hold (one commit stale; PAPERCUT-8) |
| A16 | "At 1.6 Mbps that is about 12 ms of transfer" | PLAN.md:1736–1737 | arithmetic | 1,860 B × 8 / 1.6 Mbit/s = 9.3 ms; with the shipped 2,043 B, 10.2 ms | holds within rounding |
| A17 | "Twenty-two of the seventy-one lines added to `app.js` are comments" | PLAN.md:1739; 74bf09f | unstated | `appjs-comment-count.txt` (0e03143..74bf09f): 71 added, 22 comment-only, 11 more carrying a trailing comment; cleanup got 71/22 at 67f35a5 too | holds |
| A18 | "about 2.5 KB before gzip and perhaps 900 bytes after — so roughly 60 % of the growth" | PLAN.md:1739–1740 | an estimate | comment-only lines: 2,459 B raw (holds); gzipped alone 1,148 B (an upper bound on their marginal cost); the denominator was +1,423 and is really +1,610 — 900/1,423 = 63 %, 1,148/1,610 = 71 %, and the marginal cost was never measured | can't (estimate over a stale denominator) |
| A19 | 82 shots before vs 92 after; the ten missing are five surfaces × two viewports | PLAN.md:1688–1691 | `tools/shots.js` | cleanup: `ls shots/1.12/{before,after} \| wc -l` → 82 / 92, `comm` → exactly the five names ×2 | holds |
| A20 | "Paper's [partner] is Midnight, which is what a new device starts on" | PLAN.md:1605–1606 | reading | `z-arith.txt`: `partnerOf(paper)` = midnight (holds); `SLOT_DEFAULT` = `{day: paper, night: terminal}` — a new device's **night** slot is Terminal, not Midnight | doesn't hold on the literal reading (holds if "which" means Paper) |
| A21 | "`#fill` has a half-second ease on `width`" … "one frame of `transition:none`" | PLAN.md:1628–1631 | reading | `styles.css:395` `transition:width .5s cubic-bezier(…)`; `:399` `#fill.nofade{transition:none}` | holds |
| A22 | the finale "fires 300 ms after the last check-off, so it remembers which view it fired in and cancels if the view changed" | PLAN.md:1633–1634 | reading | `app.js:1247–1249`: `setTimeout(() => { … if (… view !== viewAtCheck) { paint(); return; } … }, 300)` | holds (by code) |
| A23 | confetti: "seven bursts along the bottom 65 ms apart, a fuller one through the middle at 210 ms, and the chord at 700 ms" | PLAN.md:1640–1641; ee5a1df | reading | `fx.js:125–126`: `i * 65` for i<7 and `210` (hold); nothing at 700 in `fx.js` — cleanup PAPERCUT-5: only Arcade's pack has a chord at `t0 + 0.7`, the default packs end at 0.255 s (`h-pack-onsets.log`) | doesn't hold for "the chord at 700 ms" |
| A24 | "1.020 s end to end" (Core Haptics) | PLAN.md:1645 | the simulator self-test | Apple side; arithmetic holds (A7) | can't |
| A25 | "`test/sound.test.js` reads the volley's schedule back out of `fx.js` … re-choreographing the confetti and leaving the feeling behind is a red suite" | PLAN.md:1648–1650; ee5a1df | the test | cleanup PAPERCUT-6: mutating the run/step/centre → red; moving the chord buzz 700 → 504 ms → green | doesn't hold for the chord (holds for run and centre) |
| A26 | "Then ten seconds of Undo" | PLAN.md:1661; 67cdf4b; whatsnew 1.0 | — | `app.js:1444` `action ? 10000`; cleanup HOLDS-f `tools/f-ten-seconds.mjs` ×3: chip gone at 10,303–10,307 ms | holds |
| A27 | "`switchTo` returns its promise now … Delete this list everywhere had the same bug … fixed here" | PLAN.md:1679–1684 | the 1.12 Remove test | cleanup: Undo chip visible at +1,019 ms with two lists (fix holds); the suite's assertion passes with the `await` removed (PAPERCUT-7) | holds (the fix; its test is inert) |
| A28 | "Removal is named now … `registerList` clears the mark" — a second tab can no longer hand the list back | PLAN.md:1672–1677 | reasoning + smoke | cleanup `a-crosstab-v2.log` S5 (a second tab's save no longer restores it) but S4 / `a2-crossbuild.log` X3 (the removing tab and a 1.11 tab do) — BUG-1/2/3 | doesn't hold as a whole |
| A29 | "`closeForSwitch` had no other caller and went with it" | PLAN.md:1588; c3a189f | reading | cleanup: `git show 0e03143:app.js` → the definition and one caller | holds |
| A30 | "`openers` … has claimed since 1.4 that 'the ⋯ menu's is in this file'. It never was" | PLAN.md:1590–1591; c3a189f | reading | cleanup: comment at `0e03143:app.js:128`, no `p-menu` opener anywhere at 0e03143 | holds |
| A31 | "1.12 is stamped **158**, which is what `main` counted at the merge … About's label, the worker's cache name, `<html data-build>` and `PANELS_BUILD` all agree at 158" | PLAN.md:1773–1779; e215e65 | `git rev-list --count` | `git rev-list --count a53bde8` = 158, `67f35a5` = 159 (the record commit, as e215e65 says); cleanup HOLDS-m: all five homes 158 at 67f35a5 | holds |
| A32 | Live checks: build 158 on Pages, one list created and deleted, a fresh device's surfaces live, "no request to any host but Pages and Supabase" | PLAN.md:1747–1757; 67f35a5 | a live run | the orchestrator's; not repeated | can't |
| A33 | "A device that last saw 1.11 … the toast is the only new thing" | PLAN.md:1759–1763 | a live run | cleanup §"What a returning device sees change": holds for the first screen; the first action shows more (Remove's new sheet, Delete's now-visible Undo) | holds (first screen only) |

### B. The 1.12 commit messages (c3a189f … 67f35a5, e215e65)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| B1 | "Build 157 in all four places" | 74bf09f | the features test | `git show 74bf09f:{version.js,sw.js,index.html,panels.js,whatsnew.json}` → 157 in all five | holds |
| B2 | "the critical path carries 1.86 KB more gzipped" | 74bf09f | unstated gzip | `gzip-sizes.txt` at 74bf09f itself: critical-path delta 2,058 (l6) / 2,067 (l9) B — stale already in the commit that says it (A15) | doesn't hold |
| B3 | "medians are 52 ms apart on interleaved runs and the ranges barely touch" | 74bf09f | that script | A13 | doesn't hold as stated |
| B4 | "Twenty-two of app.js's seventy-one added lines are comments, roughly 60 % of the growth" | 74bf09f | unstated | 22/71 holds (A17); 60 % can't (A18) | holds (count) · can't (share) |
| B5 | "the five checks this round is held to, at both viewports" | cc9c7fe | the suite | cleanup: 5 new `1.12` tests per side; baseline 169/0 | holds |
| B6 | "`dialog.close()` dispatches its close event asynchronously … `popPanel` no longer closes a dialog it is about to reopen" | cc9c7fe | a bug found writing tests | the guard is in (`panels.js` restack comment, `showPanel`); the ⋯ → Theme → Back flow passes (cleanup `b-builder-back.log` B4/B5); the async-close behaviour itself not re-measured | holds (by suite) |
| B7 | "CHANGELOG.md gains 1.12 and 1.11; 1.11 shipped without an entry" | cc9c7fe | — | `git diff 0e03143..7341981 -- CHANGELOG.md` adds `## 1.12` and `## 1.11`; headings at head 1.12, 1.11, 1.10, 1.9 … | holds |
| B8 | "the last full pass was 168 and one, the one is fixed and green on both viewports on its own" | a53bde8 | one run | cannot be re-seen; head 169/0 (A4) | holds at head |
| B9 | "`git rev-list --count main` reads higher than the build from here on" | e215e65; PLAN.md:1775 | `git rev-list --count` | 67f35a5: 159 vs build 158; 7341981: 217 vs 216 | holds |
| B10 | `switchTo` … "Delete this list everywhere had the same bug" | 67cdf4b | — | A27 | holds |
| B11 | "the finale … remembers which view it fired in, so switching away during the 300 ms before the chord cancels it" | 54fb00e | reading | A22 (`viewAtCheck` guard in the 300 ms timer) | holds (by code) |
| B12 | "-TFSelfTest now builds [the pattern] regardless and prints 'ok duration=1.020s hardware=no'" | ee5a1df | the simulator | Apple side | can't |
| B13 | "A fresh device makes a list and syncs against the real backend, on build 158 … The list made for it is deleted from the server" | 67f35a5 | a live run | orchestrator's | can't |

### C. COMPATIBILITY.md — what changed in range (§5, §8)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| C1 | "the boot script in `index.html` reads three of them before any module loads" (`tf/v2/themecss`, `tf/v2/meta`, `tf/v2/themerev`) | COMPATIBILITY.md §5 (diff +85–87) | reading | `grep -oE 'localStorage\.getItem\("[^"]+"\)' index.html` → exactly those three (lines 55, 56, 63) | holds |
| C2 | "both boot scripts refuse a cache whose stamp is not the one the page carries in `<html data-tokens-rev>`" | §5; 59ef2ee | "an instrument that cannot lie" (uncommitted) | `tools/z-stamp.mjs` (Paper seeded through the page's own `theme.js`, `app.js` blocked, `?transport=local`): bogus stamp `b158` → `--ink #070A08`, `data-base=dark` (refused); no stamp → refused; current `1ptwfkh` → `--ink #F7F2E8`, `data-base=light` (trusted) — `z-stamp.txt`. `about.html` carries the identical check by diff; only `index.html` was driven | holds |
| C3 | "A missing stamp … migrated on read, never wiped" | §5 | reading | not measured (z-stamp blocks `app.js` by design, so the re-stamp by `applyTheme` was not exercised; 7341981's live run reports both caches re-stamped `1ptwfkh`) | can't |
| C4 | "`test/features.test.js` keeps `theme.js`, `index.html` and `about.html` in step … the suite fails if it does not" | §5 | the test | baseline `node-features.log:22` `ok - 1.12 b216: the cached token CSS is stamped …`; mutation: `git archive HEAD` copy with `data-tokens-rev="zzzzzzz"` in `index.html` → `node test/features.test.js` exit 1, `AssertionError: index.html's stamp is theme.js's PALETTE_REV` (`z-features-tokens-mismatch.log`) | holds |
| C5 | "`archived`, which nothing writes since 1.12 and the vault still reads" | §8 | reading | `grep -n archived app.js panels.js model.js` → reads only (`model.js:862–864` `normalizeRegistry`); cleanup HOLDS-k `Vault.swift:174` still guards on it | holds |
| C6 | "`normalizeRegistry` finishes any entry an older build left flagged, on read, once and without a word" | §8 | the 1.12 migration test | cleanup: `model.test.js` 1.12 test green at 67f35a5 and head; e2e4 lines 86/172 | holds |
| C7 | "The Swift `VaultReconciler` still skips an `archived` entry" | §8 | reasoning | cleanup HOLDS-k: `Vault.swift:174` + two tests; `apple/TodaysFiveCore` untouched by the 1.12 range | holds |

### D. README.md (unchanged in range)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| D1 | "Dark, Light, Pink plus thirteen more curated kits" | README.md What it does | — | `z-arith.txt`: `CURATED` 18, secret 2 → 16 public = 3 + 13 | holds |
| D2 | "twelve sound packs since 1.5" | README.md | — | `PACK_IDS.length` = 12 (`z-arith.txt`); 12 pack ids in `packs.js`; baseline `node-theme.log:29` "the twelve packs" | holds |
| D3 | "Every curated kit has a designed partner" | README.md | — | `gen-kits.mjs:104–108` asserts a mutual partner for all 18; 13 pairs; `z-arith.txt` partner set = 18 | holds |
| D4 | "Fonts are self-hosted; nothing loads from a third party" | README.md | the safety test | A5 | holds |
| D5 | "Idle lists cost bytes, not kilobytes, to poll" (29 bytes) | README.md; PLAN.md:1705 | real backend | can't (no backend) | can't |
| D6 | `tools/` is "the browser and real-backend suites, the dev server, the before/after screenshot tool, the CSP hash tool"; the Tests block lists five tools | README.md Files / Tests | — | `ls tools/` at head: also `kitshots.js`, `polld.js`, `mark.mjs`, `leak.mjs`, `motion.mjs`, `smoke.mjs`, `sounds.js`, `fixture.js`, `merge-fixtures.js`; README's version bullets stop at 1.9 (no 1.10/1.11/1.12); `git diff --stat 0e03143..7341981 -- README.md` is empty | doesn't hold (stale) |

### E. whatsnew.json · version.js · about.html · sw.js · index.html

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| E1 | build 216 in `version.js`, `sw.js`, `index.html data-build`, `panels.js PANELS_BUILD`, `whatsnew.json` | all five | the features test | all five read 216 (`grep`); `project.pbxproj` 6 × `CURRENT_PROJECT_VERSION = 216`; baseline `node-features.log:20` | holds |
| E2 | `BUILD` "the commit count on main at build time" — 216 | version.js:2–5; 948a905 | `git rev-list --count` | `git rev-list --count 948a905` = 216 (7341981 = 217, the record commit; 2dbdfd9 202, 16aac3d 207, 8a412a3 212, all equal to their stamped builds) | holds |
| E3 | `VERSION = "1.12"` with the build moving (e809d78's rule); pbxproj 6 × `MARKETING_VERSION = 1.12` | version.js:4; e809d78; 2dbdfd9 | — | `grep -oE 'MARKETING_VERSION = [0-9.]+' project.pbxproj \| sort \| uniq -c` → 6 × 1.12 | holds |
| E4 | `sw.js` cache `"tf-v1.12" + "-b216"` | sw.js:75–77 | features test | read; matches `version.js` | holds |
| E5 | 1.11 "New devices start on Paper by day, Terminal by night" | whatsnew.json | — | `SLOT_DEFAULT` = paper / terminal (`z-arith.txt`) | holds |
| E6 | 1.12 "Back on every panel the ⋯ menu opens" / "Theme asks for your Day theme, then your Night one" / "Everything has its own bar, count and finale" | whatsnew.json | the 1.12 e2e4 tests | baseline `ok -` lines for the three 1.12 tests per viewport; cleanup B5 (Back → `["p-menu"]`), B4/D6 (Day then Night, choice kept) | holds |
| E7 | 1.0 "delete a list everywhere with ten seconds to undo" | whatsnew.json | — | A26/A27 (the chip is now visible; ten seconds measured) | holds |
| E8 | "The public history skips 1.6 in both documents" | e809d78 | — | `whatsnew.json` versions 1.7 → 1.5; `CHANGELOG.md` headings 1.7 → 1.5 | holds |
| E9 | "Version 1.12" (static) and "Version 1.12 (build 216)" (from `whatsnew.json`) | about.html:46, 75 | — | read; `whatsnew.json` build 216 | holds |
| E10 | "The themes set text in 22 families from Google Fonts: …" (22 names) | about.html:113 (added by 1aada68) | — | `styles.css`: 26 `@font-face` blocks, 22 distinct `font-family` names, identical to the 22 the sentence lists; 26 woff2 files | holds |
| E11 | "Each family's copyright line, its reserved font name where it declares one, and the full licence text are in `fonts/OFL.txt`" | about.html:114 | — | `fonts/OFL.txt` exists (12,364 B) and names all 22 families (`grep -c` ≥ 1 each) | holds (coverage; per-family reserved names not audited) |
| E12 | "12 months" retention, "96 KB encrypted" cap, salted-hash log "at most 24 hours", "a handful of lists per hour", "a few thousand lists" | about.html:87, 97, 110 (pre-range, unchanged) | the migration | `supabase/migrations/002_v3.sql:55` `select 2400, 98304, 12, 40, interval '12 months'`; `:80` reap rate-limit rows older than 24 h. The deployed database was not read | holds against the migration (deployed state: can't) |
| E13 | `data-tokens-rev="1ptwfkh"` on both pages = `PALETTE_REV` | index.html:2, about.html:2 | features test | `z-arith.txt` `PALETTE_REV 1ptwfkh`; C4 | holds |
| E14 | CSP script-src / style-src hashes match the inline blocks; "the shared style-src hash is the same before and after" | index.html, about.html; f9e64d7; 59ef2ee | `tools/csp-hash.js` | `csp-hashes.txt`: index script (5,986 B) and style (753 B) hashes equal the meta's; about script (2,688 B) equal; the style hash `sha256-42Imb…` unchanged in `git diff 0e03143..7341981 -- about.html`, the script hash regenerated (59ef2ee) | holds |

### F. Phase 3 / Phase 4 — the web-side commit messages (branches merged into `apple-themes`, then `main`)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| F1 | "Node 137" | 948a905 | the suites | `suite-counts.txt` 948a905: **136** (features 30, theme 33) | doesn't hold |
| F2 | "Node 137" | 1aada68 | the suites | `suite-counts.txt` 1aada68: **136** | doesn't hold |
| F3 | "Node 137, swift test 130 in 10 suites" | 59ef2ee | the suites | `suite-counts.txt` 59ef2ee: **136**; swift 130 per `baseline.md` (Apple) | doesn't hold (Node) |
| F4 | "Node 136" | 8a412a3 | the suites | `suite-counts.txt` 8a412a3: **135** (features 29) | doesn't hold |
| F5 | "Node 136, swift test 130 …, e2e4 169 passed 0 failed … zero CSP violations" | fb018ce (b207) | the suites | `suite-counts.txt` fb018ce: **135**; e2e4 169/0 at head (baseline) | doesn't hold (Node) · holds (e2e4) |
| F6 | "Node: model 28, theme 33, crypto 10, sync 14, sound 12, features 29, compat 9" (=135) | 16aac3d; 0c83a5d | the suites | `suite-counts.txt` 16aac3d and 0c83a5d: exactly | holds |
| F7 | "Node: … theme 34 … features 29" (=136) | 2dbdfd9 | the suites | `suite-counts.txt` 2dbdfd9: 136, theme 34 | holds |
| F8 | "theme 34" | 3ac91b2 | the suite | `suite-counts.txt` 3ac91b2: theme 34 | holds |
| F9 | "Node 136 (model 28, theme 34, crypto 10, sync 14, sound 12, features 29, compat 9)" | 295134a (not in the predecessor's set) | the suites | `tools/z-suite-counts.sh 295134a` → 136, theme 34, features 29 (`z-suite-counts-295134a.txt`) | holds |
| F10 | "Node: … theme 32 … features 28" (=133) | 144e5b3 | the suites | `suite-counts.txt` 144e5b3: exactly | holds |
| F11 | "Node suites: … theme 33 … features 28" (=134) | 411f142 | the suites | `suite-counts.txt` 411f142: exactly | holds |
| F12 | "Node theme suite 32 -> 33" | df28946 | the suite | f9e64d7 32 → df28946 33 | holds |
| F13 | "Node theme suite 31 -> 32" | f9e64d7 | the suite | 4962f97 31 → f9e64d7 32 | holds |
| F14 | "Node features suite 28 -> 29" | ebe9ca3 | the suite | 411f142 28 → ebe9ca3 29 | holds |
| F15 | "features still holds theme.js, index.html and about.html to the same stamp"; "data-tokens-rev stays 1ptwfkh" | 948a905 | features test | `git show 59ef2ee:index.html` → `1ptwfkh`; head `1ptwfkh`; C4 | holds |
| F16 | The stamp table: "b158 stamp → --ink #070A08 (refused); no stamp → refused; current stamp → #F7F2E8 (trusted)" with `app.js` blocked | 59ef2ee | an uncommitted instrument | `z-stamp.txt` reproduces all three lines exactly (C2) | holds |
| F17 | "Both inline boot scripts changed, so both CSP script-src hashes are regenerated" | 59ef2ee | `tools/csp-hash.js` | E14 | holds |
| F18 | "truncated twelve tracked files to zero bytes … the working tree then differed from HEAD only in the 48 build-number lines" | 8a412a3 | `git diff` then | `git show --shortstat 8a412a3` → 17 files, 48 insertions, 48 deletions | holds |
| F19 | "169 on main + 42 on the branch + this one" = 212; "169 + 32 + 1 = 202"; "169 + 37 + 1" = 207 | 8a412a3; 2dbdfd9; 16aac3d | `git rev-list --count` | counts 212 / 202 / 207 (E2) | holds |
| F20 | "gen-kits.mjs … refuses to write unless 14 floors × 18 kits = 252 assertions clear" | 0c83a5d; 2dbdfd9 | the generator | `test/tools/gen-kits.mjs:73–91` `FLOORS` has 14 entries (danger/ink3 the fourteenth); 14 × 18 = 252 | holds |
| F21 | "18 kits, the 13 font pairs … 234 contrast assertions cleared. The tightest is Light's `dim` on its own `--ink`, 4.5002" | 9a2f369 | the generator then | 13 × 18 = 234 (thirteen floors before 2dbdfd9); `z-arith.txt` `contrast(light.dim, light.ink)` = **4.5002** | holds |
| F22 | "light's danger #B13924 at 4.5050 … now the worst of the eighteen" (danger on `--ink-3`) | 2dbdfd9 | `report()` | `contrast.txt`: lowest three light 4.5050, blush 4.5142, sunset 4.5324 | holds |
| F23 | light's danger "on --ink-3 4.1205 -> 4.5050; on --ink-2 4.6907 -> 5.1284; on --ink 5.2034 -> 5.6889" | 411f142; theme.js comment (diff +1107–1111) | `contrast()` | `contrast.txt`: identical to four decimals | holds |
| F24 | "terminal accent on --ink 4.12 -> 13.29, on --ink-3 3.48 -> 11.22; paper 4.33 -> 4.79, 3.48 -> 3.85; dark 4.12 / 3.48 unchanged" | f9e64d7; PLAN-apple-phase4.md:434–437 | `contrast()` | `contrast.txt`: 13.2924 / 11.2212, 4.7867 / 3.8477, 4.1178 / 3.4762, and 4.3284 / 3.4793 for #A86014 on Paper | holds |
| F25 | "light's accent on --ink-3 is 3.0016 and Pink's accent text on --ink-3 is 4.5069" | f9e64d7; test/theme.test.js (diff +951–953) | the test | `contrast.txt`: 3.0016, 4.5069; baseline `node-theme.log:27` | holds |
| F26 | "cssText() moved on exactly two of the eighteen kits; the other sixteen are byte-identical" | f9e64d7 | a diff then | `z-shapes-and-hex.txt`: 0e03143 → HEAD, `colors` differ for **paper, terminal and light** (light's danger via 411f142); 15 of 18 identical. True at f9e64d7, before 411f142 | holds at its commit · stale at head |
| F27 | "Dark keeps #A86014"; "BRAND_ACCENT … untouched … no kit's hex is written into the SVG" | f9e64d7 | the test | `contrast.txt` `BRAND_ACCENT = #A86014`; dark's accent 4.1178 on ink = #A86014; the SVG check is the Ink track's | holds (accent) · Ink (SVG) |
| F28 | "`derive()` … 1,139 of 3,000 under 3:1 on dark, 1,690 on light; worst 2.18 / 2.45; after 0 of 3,000" and "danger … 1,222 of 3,000 … 0 of 3,000, worst 4.5001 / 4.9517" | df28946; 411f142; 0c83a5d; theme.js:542–543; DECISIONS-apple.md:958–964, 998–999 | a test on `derive-ink3-next` (`3adf295`, not on main) | not re-run (the Ink track owns the figure). Noted: `apple/PLAN-apple-phase4.md:157–158` states **1,152 and 1,687** of 3,000 for the same measurement | can't (Ink) — two write-ups disagree |
| F29 | "The union stops at the generator … fifteen of the eighteen kits reached Swift reading as hearts-only … the fifteen read [0] instead of [1]" | 3ac91b2 | reading | `z-shapes-and-hex.txt`: fixture at `3ac91b2^` `shapes` = `[1]` ×**12**, `[3]` ×3, `[2]` ×1, explicit lists ×2 — twelve read hearts-only, sixteen were wrong; at 3ac91b2 and head `[0]` ×12, `[0,1,2]` ×3, `[0,1]` ×1 | doesn't hold (twelve, not fifteen) |
| F30 | "62 of the 314 hex tokens in the finished table appear nowhere in theme.js's source and every one of the 18 kits has at least one" | 144e5b3; test/theme.test.js:994–995; DECISIONS-apple.md:1078 | a count then | `z-shapes-and-hex.txt`: at 144e5b3 → 314 / **62** / 18 (holds when written); at 3ac91b2 and head → 314 / **58** / 18 (f9e64d7 put Paper's and Terminal's accent families back into the source as literals) | holds at its commit · doesn't hold at head (stale comment) |
| F31 | "33 TTF, 1.18 MB from 688 KB"; "33 files, not 26" | 9a2f369 | the generator | `test/fixtures/watch-fonts.json` bytes sum 1,238,128 = 1.18 MiB (holds); 33 faces (holds); `git ls-tree -r -l 9a2f369 -- fonts/`: 26 woff2, **732,924 B = 716 KiB = 733 kB** — not 688 under either unit | doesn't hold (688 KB) |
| F32 | "**12** of the 22 families are not called what `theme.js` calls them, not 10" | 9a2f369 (vs PLAN-apple-phase4.md:459 "ten of the twenty-two … resolve to Helvetica") | `CTFontCreateWithName` | the criterion is unstated; on the fixture, 20 of 22 CSS families have at least one face whose TTF family name is not the CSS name verbatim (`z-shapes-and-hex.txt`). The two write-ups still disagree (ten vs 12); the Apple track owns the resolution | can't (definition) — write-ups disagree |
| F33 | "Ten faces carry a Reserved Font Name" | 9a2f369 | reading the binaries | superseded by 1aada68: 15 (F34) | doesn't hold (known-wrong, corrected in range) |
| F34 | "The repo said 10 faces across 7 families; it is 15 across 9" | 1aada68 | the generator | `z-arith.txt`: 33 faces, `reservedFontName: true` on **15**, grouped by source woff2 family → **9** (ibm-plex-mono, ibm-plex-sans, josefin-sans, lato, lora, playfair-display, pt-sans, quicksand, source-serif-4) | holds |
| F35 | "All 22 families are OFL 1.1"; "OFL.txt … at fonts/OFL.txt on the web"; "about.html gains a Typefaces section" | 1aada68 | an audit | `fonts/OFL.txt` present and names 22 families; `about.html:112–114` added in range (E10/E11). Licence type per family not re-audited | holds (presence) |
| F36 | "PT Sans … licence URL http://scripts.sil.org/OFL_web, which 404s" | 1aada68 | a fetch | not fetched (no network by rule) | can't |
| F37 | `tools/kitshots.js`: "18 kits in 9.4 s on this machine, no network, no list creations, zero page errors" | 39b58af | one run | predecessor's one run: 18 `shot` lines, `KITSHOTS … wall=9.264893 s`, exit 0 — and the script exits 1 and prints `page errors:` when any occur (`kitshots.js:70–72`), so exit 0 is the zero-error claim (`kitshots.log`, `kitshots-timing.txt`) | holds (one run then, one run now) |
| F38 | "The medians (132 s and 16 s) differ from the scout's (54 s and 32 s)"; "Fifty trials take three minutes" | f4c5faa | `tools/polld.js` | scout's table `apple/PLAN-apple-phase4.md:89–90`; `polld.js:203–207` `median()` is the standard mid-mean, and over the scout's own listed trials it gives **50 and 29**, not 54 and 32. Re-run now, ten trials each, three seeds (`z-polld-summary.txt`): fg-live **83 / 131 / 100 s** (max 210/198/226), fg-dead **47 / 32 / 35 s**; each ten-trial invocation took 16–27 s wall, so fifty ≈ 1.5–2 min | doesn't hold (the medians, in every write-up) · holds (the shape: ratio ≈ 3–4×) |
| F39 | "the ratio between the two foreground conditions is POLL_LIVE_MS / POLL_MS … four times slower"; "schedulePoll's visible() gate is upstream of the timer" | f4c5faa; polld.js:36–42 | reading | `sync.js:215–216` 240000 / 60000 = 4; `sync.js:385, 392` `visible()` gates the pull and the reschedule; my 30-trial means 107.9 s vs 33.7 s (3.2×, sampling) | holds |
| F40 | "backgrounded, either way — never, 20/20 in 600 s of virtual time" | f4c5faa; PLAN-apple-phase4.md:91 | polld | not re-run (the two `bg-*` conditions cost 600 s virtual each trial); the gate (F39) makes it arithmetic | can't (not re-run) |
| F41 | polld "answered with a clean table 'never, 0/0' … exit 0" → now "NOT MEASURED … exits 1" | c0b8e2b | — | `tools/polld.js:259` `"NOT MEASURED"`, `:268` `process.exit(1)`; my six runs exited 0 with measured trials | holds |
| F42 | "theme.js said the Secret word was not written down, and that was false — `tools/shots.js` types it" | b95ab1b | reading | `theme.js:487–496` now says it *is* written down elsewhere in the repo; `tools/shots.js` has six `secret` mentions (the word is not quoted here) | holds |
| F43 | Yours "without the key … rendered 2 swatches; after 1; with the key, 2, and the group's 2" | ebe9ca3 | a browser measurement then | not re-driven; the guard is held by the Node features test that lifts `savedThemes()` by regex (baseline `node-features.log`) | can't (swatch counts) · holds (the gate, by test) |
| F44 | "four build homes plus the sixth … project.pbxproj's six CURRENT_PROJECT_VERSION lines"; "the 1.8 wink from nth-child(5)" | e809d78 | — | E1; `grep -c 'nth-child(5)' tools/e2e4.js` = 2 | holds |
| F45 | "`mark.mjs --trace` … reports **0.30 %** against a 2 % threshold" (was 93.84 %) | 7a8936c; 295134a | the tool | not run (the Ink track's) | can't (Ink) |
| F46 | "fifteen of eighteen kits are byte-identical. Paper and Terminal get their own accent back … Light's danger token shifts by at most 7/255" | e6f7d28; PLAN-apple-phase4.md:548–556 | a diff | `z-shapes-and-hex.txt`: 15 of 18 `colors` identical 0e03143 → head; #B8402A → #B13924 = (7, 1, 6)/255 | holds |
| F47 | "a one-frame flash of the **old accent** on a returning device … because the boot script paints the cached token CSS before any module loads" | e6f7d28 (b207); PLAN-apple-phase4.md:566–569 | reasoning | superseded by 59ef2ee: a cache with no stamp (what a b158 device has) is **refused**, so the first frame is the **inline default** (Dark's tokens: `--ink #070A08`, `data-base=dark`, `z-stamp.txt` "no stamp" row) — for a Paper device that is a dark frame, not the old accent on cream. The paragraph was not updated after the stamp landed | doesn't hold at head (stale) |
| F48 | "last ran b158 on Terminal → paints #4AF07A … on Paper → paints #C8321F, ground and text unchanged, 0 errors … both caches end up … stamped 1ptwfkh" | 7341981; PLAN-apple-phase4.md:590–600 | a run against the deployed site | orchestrator's; the local mechanism (refuse, then `theme.js` paints the kit) holds (C2); "ground unchanged" is after modules ran, not the first frame (F47) | can't (live) |
| F49 | "0.63 seconds where the same conditions used to mean 240 … 29 bytes … page errors 0 … three lists created, all three confirmed gone" | 5cd559f; PLAN-apple-phase4.md:513–540 | a live run | real backend | can't |
| F50 | "Both targets: BUILD SUCCEEDED, zero warnings, zero errors"; "Phase 3's standard is zero Swift warnings" | a902805; fb018ce; PLAN-apple-phase4.md:424 | xcodebuild | Apple track (baseline: five `SUCCEEDED`) | can't (Apple) |
| F51 | "six of the eighteen kits are light" | e3255d0 | reading | fixture: `base: light` = **7** (light, paper, harbor, blush, teletype, sketch, birthday); `lean: day` = 9 | doesn't hold (seven or nine, by either definition) |
| F52 | "The screenshots were asked for as evidence and turned out to be a test" — eighteen Watch frames found the carousel platter bug | e3255d0; DECISIONS-apple.md:1722–1785 | eighteen screenshots | Apple track; the web analogue `tools/kitshots.js` produces 18 PNGs and asserts nothing (F37) | can't (Apple) |
| F53 | "the web is untouched" by Phase 3 | apple/PLAN-apple-phase3.md:618 | — | `git log 67f35a5..4962f97 -- . ':!apple'` → only e215e65 (PLAN.md prose) | holds |

### G. Code comments stating figures (at HEAD, added or kept in range)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| G1 | "#A86014 … clears the 3:1 floor on all four grounds by 16 % (min 3.48) … #D9A066 … is 2.05:1 on Paper" | theme.js:147–150 | `contrast()` | `contrast.txt`: min 3.4762 on Terminal ink3 (15.9 %), 2.0523 | holds |
| G2 | "1,139 of 3,000 under the 3:1 floor … 1,690 of 3,000" | theme.js:542–543 (derive's note) | F28 | Ink track; disagrees with PLAN-apple-phase4.md:157–158 | can't (Ink) |
| G3 | "#B8402A was 4.12:1 on this kit's --ink-3 … 4.1205 → 4.5050 on --ink-3, 5.2034 → 5.6889 on --ink" | theme.js (light kit comment) | `contrast()` | F23 | holds |
| G4 | "light's accent on --ink-3 by 0.0016 and Pink's accent text on --ink-3 by 0.0069" | test/theme.test.js | the test | F25 | holds |
| G5 | "62 of the 314 hex tokens come out of finalize()/ensure()/oklch() and appear nowhere in the source" | test/theme.test.js:994–995 | F30 | 58 at head | doesn't hold at head (stale) |
| G6 | "the version is one number in three places, the build in four"; "<html> gained data-tokens-rev in 1.12 b216" | test/features.test.js | the test | baseline `node-features.log:20, 22`; E1 | holds |
| G7 | "seven bursts at i × 65 ms, the centre burst at 210 ms inside the fourth, and the chord at 700 ms" | sound.js:16–17, 103–104 | reading | A23: 65 / 210 hold; 700 is one pack of twelve | doesn't hold (700) |
| G8 | "the chord (300 ms after the last check-off)" | app.js:139, 1246 | reading | A22 | holds |
| G9 | `const VERSION = "tf-v1.12"; const BUILD = 216;` "= version.js's BUILD" | sw.js:75–76 | features test | E1/E4 | holds |
| G10 | `PANELS_BUILD = 216` "stamped with version.js, checked by test/features.test.js" | panels.js:6 | features test | E1 | holds |
| G11 | "POLL_MS (60 s) to POLL_LIVE_MS (240 s) … four times slower" | tools/polld.js:36–42 | reading | F39 | holds |
| G12 | "four minutes of waiting costs a few hundred milliseconds and forty trials cost about six" | tools/polld.js:6–8 | — | unit unstated; measured 16–27 s wall per ten-trial invocation, so forty ≈ 70–100 s — not six seconds, not six minutes | doesn't hold (ambiguous) |
| G13 | "the Secret key is never typed here … CURATED already holds them and applyTheme does not ask" | tools/kitshots.js header | reading | `kitshots.js:52–66` loops `T.CURATED` with `persist:false`; predecessor's run shot superpink and birthday (`kitshots.log`) | holds |
| G14 | "1.12: the ⋯ menu sits under the stack, so closing it takes more than one Escape" | tools/e2e4.js (nine sites) | the suite | cleanup HOLDS-l: `t.esc()` loops up to five times; 169/0 | holds |
| G15 | "the thirty-odd code comments the tracks tagged 1.13 resolve to '1.12 b202'" | 2dbdfd9 | — | `git grep -c '1\.12 b216'` → 40 across the repo (29 web-side, 11 apple); `git grep '1\.13' -- . ':!apple'` → none | holds (now stamped b216) |
| G16 | `normalizeRegistry` "finishes any entry an older build left flagged … The vault reads a dropped entry exactly as it read an archived one" | model.js:858–864 | the test | C5/C6 | holds |

### H. `apple/PLAN-apple-phase4.md` — the web-side figures in its Results (the Phase 4 "PLAN" entry)

| # | claimed | where | then | now | verdict |
|---|---|---|---|---|---|
| H1 | Node "**136 tests** — model 28, theme 33, crypto 10, sync 14, sound 12, features 29, compat 9" | :421 | the suites | the seven listed **sum to 135**; measured 135 at fb018ce/e6f7d28 (b207); 136 only after 59ef2ee (features 30) | doesn't hold (wrong sum) |
| H2 | baseline "Node **132 tests** across seven suites (… theme 31 … features 28)" | :27 | the suites | `suite-counts.txt` 4962f97: 132 | holds |
| H3 | `tools/e2e4.js` "169 passed, 0 failed at 1440×900 and 390×844, with zero page errors, zero CSP violations and zero third-party requests" | :423 | the suite | baseline | holds |
| H4 | checkpoint medians "**54 s** (max 230)" over 6, 26, 32, 44, 46, 54, 174, 186, 218, 230 and "**32 s** (max 58)" over 8, 8, 10, 14, 26, 32, 36, 52, 56, 58 | :89–90 | polld (pre-mulberry32) | `polld.js` `median()` over those lists → 50 and 29; re-run F38 | doesn't hold (internally) |
| H5 | "Three runs of the same condition gave 54 s, 132 s and 144 s" | :487; fb018ce; DECISIONS-apple.md:1929–1939 | three runs | the three are the scout's clustered-LCG run, f4c5faa's seed and DECISIONS-apple's second seed — three harness states, not three runs of one instrument; my three seeds of the committed instrument: 83, 131, 100 s | doesn't hold as "three runs" · holds (the point: the median is not stable) |
| H6 | "Fifteen of the eighteen kits are byte-identical … the cssText() diff over the whole table moves **exactly two** of them" | :548–549 | a diff | 15 identical holds (F46); **three** kits' colours moved (light's danger too) — "exactly two" is f9e64d7's pre-411f142 number | doesn't hold (as written) |
| H7 | "One frame, once per device … shows the old accent" | :566–569 | reasoning at b207 | F47 | doesn't hold at head |
| H8 | "33 static faces, **1.3 MB**" | :456 | — | fixture bytes 1,238,128 = 1.18 MiB = 1.24 MB (9a2f369 says 1.18 MB) | doesn't hold (1.2 by either unit) |
| H9 | "ten of the twenty-two CSS names resolve to Helvetica" | :459 | `CTFontCreateWithName` | F32: 9a2f369 corrected it to 12; this line was not updated | doesn't hold (superseded in range) |
| H10 | "Surprise me over 2,000 themes moved 0, and the four real-shaped T2: codes in the repo moved 0. The ~47 % figure came from uniform-random hex" | :506–507; fb018ce | an uncommitted script | no such instrument in `tools/` or `test/tools/` (`git log --all -- tools/ test/tools/`); the Ink track owns the figure | can't (Ink; uncommitted) |
| H11 | "138 bytes per successful write, zero at idle, and the 29-byte unchanged poll does not move" | :495–496; DECISIONS-apple.md:1897–1906 | the live run | real backend | can't |
| H12 | The returning-device table (Terminal → #4AF07A, Paper → #C8321F, 0 errors, caches re-stamped) | :590–600 | the deployed site | F48 | can't (live) |

## Traps found

- **One-answer instruments.** `tools/kitshots.js` (39b58af) can only fail on a page error; the question it was written for — "does #C8321F still read as this product on Paper's cream" — is answered by a person looking at 18 PNGs, and DECISIONS-apple.md:899 says as much ("the render pass … is what settled the change"). The two 1.12 browser assertions cleanup found inert ("repainted on the way back", "not hidden behind the switch", PAPERCUT-7) are the same shape inside the suite. `tools/mark.mjs --trace` was the extreme case until 7a8936c: it had reported 93.84 % since 1.11 and nothing ran it, so a broken guard read as nothing. The pre-c0b8e2b `polld.js` printed "never, 0/0 … exit 0" for a dead server. Fixed in range for polld (F41); still true of kitshots by design.
- **Single-run medians.** The **54 s** (PLAN-apple-phase4.md:89) is ten trials from a harness whose LCG phases "are visibly clustered" (f4c5faa); by the committed `median()` its own ten values give 50. The write-ups then quote "54, 132 and 144" as three runs of one condition (H5) when they are three harness states. Three seeds of the committed instrument today: 83 / 131 / 100 s (F38). The PLAN.md first-paint table is not single-run (eight rounds) but its headline "52 ms apart, ranges barely touch" was one session's: the re-run gives 26 ms with a 28 ms overlap (A13). "18 kits in 9.4 s" is one run then and one run now (F37).
- **Wrong-list counts.** "Node 137" three times (F1–F3) and "Node 136" twice (F4, F5) against measured 136 and 135; PLAN-apple-phase4.md:421 lists seven suites that sum to 135 under a "136" (H1). "Fifteen of the eighteen kits … hearts-only" is twelve (F29). "Six of the eighteen kits are light" is seven (F51). "62 of the 314" was right at 144e5b3 and is 58 at head, in a test comment that still says 62 (F30/G5).
- **Fragile denominators.** "Roughly 60 % of the growth" divides an estimate ("perhaps 900 bytes") by a number that was already stale in the commit that wrote it (+1,423 → +1,610; A18). "A little under 4 %" is 26/1144 = 2.3 % on the re-run (A14). "Ten of the twenty-two" vs "12 of the 22" families (F32/H9) is a count whose criterion is never written down, so it cannot be re-derived.
- **Uncommitted scripts.** The CDP first-paint instrument behind PLAN.md:1714–1727 was never committed (A9; `git log --all -- tools/` and the `-S` search agree with cleanup PAPERCUT-9). Also uncommitted: 59ef2ee's stamp instrument (re-implemented here as `z-stamp.mjs`, F16), the "Surprise me, 2,000 themes" script (H10), the 3,000-seeded-accents harness (it lives as a test on `derive-ink3-next`, `3adf295`, not on main; F28), and 7341981's returning-device seeding script (it touched only `apple/PLAN-apple-phase4.md`).
- **Stale numbers.** PLAN.md's byte table (A15); README.md's `tools/` list and version bullets (D6); `test/theme.test.js:994` "62 of the 314" (G5); PLAN-apple-phase4.md "exactly two" kits (H6), "ten of the twenty-two" (H9), "1.3 MB" (H8) and the "old accent for one frame" paragraph (H7) — all overtaken by later commits in the same range. Not stale: `version.js` 216 = the commit count at 948a905 (E2), `sw.js` `tf-v1.12-b216` (E4), `whatsnew.json` 216 (E1), about.html's 22 families (E10), `data-tokens-rev` 1ptwfkh (E13).
- **Screenshots as test.** e3255d0's eighteen Watch frames were asked for as evidence and found a bug three frames had not (Apple side); the web's `tools/kitshots.js` is the same method with no assertion, and PLAN.md's 82/92 `shots/1.12` sets are the before/after record, checked by count only (A19).
- **"Zero warnings" / "all green".** fb018ce and PLAN-apple-phase4.md:424 name the Xcode builds (Apple); a902805 names both Watch targets; PLAN.md:1704 names the browser suite's three safety assertions (holds at head, A5); baseline.md names the seven Node suites ("0 warnings"); PLAN-apple-phase3.md:618 "all green" names the seven Node tallies (holds, H2). None names `tools/kitshots.js` or `polld.js`, which is right.
- **"Ten seconds."** Every place it appears web-side — PLAN.md:1661/1679–1683, 67cdf4b, README ("ten-second undo"), whatsnew 1.0, `panels.js` comments, `app.js:1444` — cleanup measured it (HOLDS-f) and this track did not redo it (A26/E7).
- **The `live/` fetch.** Recorded in the header; not a measurement anything here rests on.

## Known-wrong items — what became of each

- **The 54 s median.** Born in the Phase 4 checkpoint table (`apple/PLAN-apple-phase4.md:89`, "54 s (max 230)"), where by the harness's own `median()` the listed trials give 50. f4c5faa replaced the LCG with mulberry32 and reported 132 s / 16 s, calling neither the finding; DECISIONS-apple.md:1927–1941 added a second seed (144 s / 12 s) and wrote "'54 seconds' survives none of them"; fb018ce and PLAN-apple-phase4.md:487 then folded the three into "three runs of the same condition gave 54, 132 and 144", which is three harness states. Nothing web-side still quotes 54 s as a finding; `SyncEngine.swift:258` (Apple) quotes 132 s. Three-run figure now, committed instrument, loopback only: **fg-live 83 / 131 / 100 s, fg-dead 47 / 32 / 35 s** (`z-polld-*.log`). The number that reproduces is the mechanism (uniform on the period; 240 s vs 60 s), not any median.
- **Reserved-name count 10/7 → 15/9 (1aada68).** Web-side homes of the number: `test/fixtures/watch-fonts.json` (`reservedFontName: true` on 15 of 33 faces, grouped by source family → 9: F34, holds) and `about.html:114` (no count, links `fonts/OFL.txt`). The old "ten faces" survives only in the 9a2f369 commit message (F33). `model.js`, `PLAN.md`, `COMPATIBILITY.md` and the tests never carried the reserved-name count — the brief's "model.js, tests, PLAN, COMPATIBILITY" turn up nothing for `reserved` web-side (`git grep -n -i reserved HEAD -- model.js test/ PLAN.md COMPATIBILITY.md README.md about.html whatsnew.json` → the fixture and about.html only). `apple/TodaysFive/Fonts/README.md` is the Apple track's.
- **"0 of 2,000 themes."** Web-side it appears nowhere at head (`git grep -n -E '2,000' HEAD -- . ':!apple'` → only PLAN.md:1196's unrelated 1.8 row); it lives in the fb018ce message ("Surprise me, 2,000 themes — it is zero") and `apple/PLAN-apple-phase4.md:506`. Its neighbour, the 3,000-seeded-accents figure, is web-side in `theme.js:542–543` (1,139 / 1,690 of 3,000) and disagrees with `apple/PLAN-apple-phase4.md:157–158` (1,152 / 1,687). The Ink track owns both figures; the derive() change they justify was pulled to `derive-ink3-next` (0c83a5d) and is not at head. The baseline theme suite's "custom derivation meets the floors for 2000 random accents" (`node-theme.log:33`) is an older, different test.
- **PLAN.md's byte table.** Cleanup found it (PAPERCUT-8); the predecessor's zlib measurement and cleanup's `gzip -9` agree the shipped `app.js` grew ~1,610–1,629 B, not 1,423 (A15/B2). Unchanged at head.
- **The Lighthouse-substitute script.** Cleanup found it absent (PAPERCUT-9); confirmed (A9). The predecessor's `firstpaint.mjs` is the only version of that instrument that exists, and it reproduces the direction, not the magnitude (A12–A14).

## Can't verify here — what would

- **Anything against the real backend** (A6, D5, F49, H11, H12, F48, A32, B13): one `tools/realsync4.js` run (it creates and deletes several lists), one seeded returning-device open against the deployed site, one `tfive add` timing — the orchestrator's single live run.
- **The Apple half** (A2, A7, A24, B12, F45, F50, F52): `swift test`, the phone self-test on a simulator, `tools/mark.mjs --trace`, `xcodebuild` — the Apple and Ink tracks.
- **The original first-paint table's basis** (A9, A11): only the author's uncommitted script could say what "on the wire" summed; the stand-in already exists — committing `firstpaint.mjs` (or the original) to `tools/` would make the table repeatable.
- **The 60 % comment share** (A18): `gzip -9` of `app.js` at 74bf09f with and without the 22 comment lines stripped (`sed` on a copy) gives the marginal cost in one command; not done because the denominator moves with it and the argument survives either way.
- **"ten of the twenty-two" vs "12 of the 22"** (F32/H9): state the criterion (which name `Font.custom` must be given) and count against `watch-fonts.json` — the Apple track.
- **"never, 20/20"** (F40): `node tools/polld.js --only bg-live --trials 10` and `bg-dead` — about twenty minutes of wall time for twenty 600 s-virtual trials, skipped under the load rule.
- **The stamp's migration on read** (C3): one more `z-stamp` case with `app.js` unblocked, reading `tf/v2/themerev` after `theme.js` runs.
- **PT Sans's 404** (F36): one HTTP HEAD — a network fetch, outside this track's rules.
